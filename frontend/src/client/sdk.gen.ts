// This file is auto-generated by @hey-api/openapi-ts

import { type Options as ClientOptions, type TDataShape, type Client, urlSearchParamsBodySerializer } from './client';
import type { LoginLoginAccessTokenData, LoginLoginAccessTokenResponses, LoginLoginAccessTokenErrors, LoginTestTokenData, LoginTestTokenResponses, LoginRecoverPasswordData, LoginRecoverPasswordResponses, LoginRecoverPasswordErrors, LoginResetPasswordData, LoginResetPasswordResponses, LoginResetPasswordErrors, LoginRecoverPasswordHtmlContentData, LoginRecoverPasswordHtmlContentResponses, LoginRecoverPasswordHtmlContentErrors, UsersReadUsersData, UsersReadUsersResponses, UsersReadUsersErrors, UsersCreateUserData, UsersCreateUserResponses, UsersCreateUserErrors, UsersDeleteUserMeData, UsersDeleteUserMeResponses, UsersReadUserMeData, UsersReadUserMeResponses, UsersUpdateUserMeData, UsersUpdateUserMeResponses, UsersUpdateUserMeErrors, UsersUpdatePasswordMeData, UsersUpdatePasswordMeResponses, UsersUpdatePasswordMeErrors, UsersRegisterUserData, UsersRegisterUserResponses, UsersRegisterUserErrors, UsersDeleteUserData, UsersDeleteUserResponses, UsersDeleteUserErrors, UsersReadUserByIdData, UsersReadUserByIdResponses, UsersReadUserByIdErrors, UsersUpdateUserData, UsersUpdateUserResponses, UsersUpdateUserErrors, UtilsTestEmailData, UtilsTestEmailResponses, UtilsTestEmailErrors, UtilsHealthCheckData, UtilsHealthCheckResponses, NetworkApiTestProxyData, NetworkApiTestProxyResponses, NetworkApiTestProxyErrors, NetworkApiProxyHealthData, NetworkApiProxyHealthResponses, NetworkApiTorStatusData, NetworkApiTorStatusResponses, NetworkApiTorIpData, NetworkApiTorIpResponses, NetworkApiTorRestartData, NetworkApiTorRestartResponses, NetworkApiTorNewIdentityData, NetworkApiTorNewIdentityResponses, NetworkApiTorNewIdentityErrors, TelegramApiScrapeData, TelegramApiScrapeResponses, TelegramApiScrapeErrors, TelegramApiChannelInfoData, TelegramApiChannelInfoResponses, TelegramApiChannelInfoErrors, TelegramApiResolveStartTimeData, TelegramApiResolveStartTimeResponses, TelegramApiResolveStartTimeErrors, TelegramApiBotInfoData, TelegramApiBotInfoResponses, TelegramApiBotInfoErrors, TelegramApiPublishData, TelegramApiPublishResponses, TelegramApiPublishErrors, TelegramApiChannelPhotoData, TelegramApiChannelPhotoResponses, TelegramApiChannelPhotoErrors, TelegramApiPostThumbData, TelegramApiPostThumbResponses, TelegramApiPostThumbErrors, TelegramApiBotFileData, TelegramApiBotFileResponses, TelegramApiBotFileErrors, AiApiListModelsData, AiApiListModelsResponses, AiApiSummaryData, AiApiSummaryResponses, AiApiSummaryErrors, AiApiSummaryPromptData, AiApiSummaryPromptResponses, AiApiSummaryPromptErrors, AiApiSummaryStreamData, AiApiSummaryStreamResponses, AiApiSummaryStreamErrors, AiApiChatStreamData, AiApiChatStreamResponses, AiApiChatStreamErrors, AiApiTagPromptData, AiApiTagPromptResponses, AiApiTagPromptErrors, AiApiTagStreamData, AiApiTagStreamResponses, AiApiTagStreamErrors, AiApiEmbeddingsData, AiApiEmbeddingsResponses, AiApiEmbeddingsErrors, AiApiTranslateData, AiApiTranslateResponses, AiApiTranslateErrors, DataGetSyncMetaRouteData, DataGetSyncMetaRouteResponses, DataListChannelsData, DataListChannelsResponses, DataListChannelsErrors, DataListChannelStatsData, DataListChannelStatsResponses, DataListChannelBiosData, DataListChannelBiosResponses, DataDeleteChannelData, DataDeleteChannelResponses, DataDeleteChannelErrors, DataUpsertChannelData, DataUpsertChannelResponses, DataUpsertChannelErrors, DataStartBulkFollowData, DataStartBulkFollowResponses, DataStartBulkFollowErrors, DataGetBulkFollowStatusData, DataGetBulkFollowStatusResponses, DataGetBulkFollowStatusErrors, DataBulkFollowEventsData, DataBulkFollowEventsResponses, DataBulkFollowEventsErrors, DataCancelBulkFollowData, DataCancelBulkFollowResponses, DataCancelBulkFollowErrors, DataBulkReresolveStartIdsEndpointData, DataBulkReresolveStartIdsEndpointResponses, DataBulkReresolveStartIdsEndpointErrors, DataBulkResetSyncEndpointData, DataBulkResetSyncEndpointResponses, DataBulkResetSyncEndpointErrors, DataBulkSyncSettingsEndpointData, DataBulkSyncSettingsEndpointResponses, DataBulkSyncSettingsEndpointErrors, DataListSettingGroupsData, DataListSettingGroupsResponses, DataCreateSettingGroupData, DataCreateSettingGroupResponses, DataCreateSettingGroupErrors, DataDeleteSettingGroupData, DataDeleteSettingGroupResponses, DataDeleteSettingGroupErrors, DataUpdateSettingGroupData, DataUpdateSettingGroupResponses, DataUpdateSettingGroupErrors, DataBulkAssignSettingGroupData, DataBulkAssignSettingGroupResponses, DataBulkAssignSettingGroupErrors, DataBulkChannelTagsEndpointData, DataBulkChannelTagsEndpointResponses, DataBulkChannelTagsEndpointErrors, DataGetChannelStatsData, DataGetChannelStatsResponses, DataGetChannelStatsErrors, DataListPostsData, DataListPostsResponses, DataListPostsErrors, DataPostsCountsData, DataPostsCountsResponses, DataPostsCountsErrors, DataLookupPostsRouteData, DataLookupPostsRouteResponses, DataLookupPostsRouteErrors, DataBulkUpsertPostsRouteData, DataBulkUpsertPostsRouteResponses, DataBulkUpsertPostsRouteErrors, DataDiscoverCandidatesData, DataDiscoverCandidatesResponses, DataDiscoverCandidatesErrors, DataRemoveDiscoverIgnoredData, DataRemoveDiscoverIgnoredResponses, DataRemoveDiscoverIgnoredErrors, DataListDiscoverIgnoredData, DataListDiscoverIgnoredResponses, DataAddDiscoverIgnoredData, DataAddDiscoverIgnoredResponses, DataAddDiscoverIgnoredErrors, DataListDiscoverProbesData, DataListDiscoverProbesResponses, DataListDiscoverProbesErrors, DataGetDiscoverProbeQueueData, DataGetDiscoverProbeQueueResponses, DataRecheckDiscoverProbesData, DataRecheckDiscoverProbesResponses, DataRecheckDiscoverProbesErrors, DataListDiscoverReportsData, DataListDiscoverReportsResponses, DataListDiscoverReportsErrors, DataCreateDiscoverReportData, DataCreateDiscoverReportResponses, DataCreateDiscoverReportErrors, DataDeleteDiscoverReportData, DataDeleteDiscoverReportResponses, DataDeleteDiscoverReportErrors, DataGetDiscoverReportData, DataGetDiscoverReportResponses, DataGetDiscoverReportErrors, DataUpdateDiscoverReportFlagsData, DataUpdateDiscoverReportFlagsResponses, DataUpdateDiscoverReportFlagsErrors, DataListSummariesData, DataListSummariesResponses, DataListSummariesErrors, DataDeleteSummaryData, DataDeleteSummaryResponses, DataDeleteSummaryErrors, DataGetSummaryData, DataGetSummaryResponses, DataGetSummaryErrors, DataUpsertSummaryData, DataUpsertSummaryResponses, DataUpsertSummaryErrors, DataListTagRunsData, DataListTagRunsResponses, DataListTagRunsErrors, DataDeleteTagRunData, DataDeleteTagRunResponses, DataDeleteTagRunErrors, DataGetTagRunData, DataGetTagRunResponses, DataGetTagRunErrors, DataUpsertTagRunData, DataUpsertTagRunResponses, DataUpsertTagRunErrors, DataListChatSessionsData, DataListChatSessionsResponses, DataListChatSessionsErrors, DataDeleteChatSessionData, DataDeleteChatSessionResponses, DataDeleteChatSessionErrors, DataGetChatSessionData, DataGetChatSessionResponses, DataGetChatSessionErrors, DataUpsertChatSessionData, DataUpsertChatSessionResponses, DataUpsertChatSessionErrors, DataListArtifactsData, DataListArtifactsResponses, DataListArtifactsErrors, DataListBotCredentialsData, DataListBotCredentialsResponses, DataDeleteBotCredentialData, DataDeleteBotCredentialResponses, DataDeleteBotCredentialErrors, DataUpsertBotCredentialData, DataUpsertBotCredentialResponses, DataUpsertBotCredentialErrors, DataMigrateBotCredentialsData, DataMigrateBotCredentialsResponses, DataMigrateBotCredentialsErrors, DataListChatDestinationsData, DataListChatDestinationsResponses, DataDeleteChatDestinationData, DataDeleteChatDestinationResponses, DataDeleteChatDestinationErrors, DataUpsertChatDestinationData, DataUpsertChatDestinationResponses, DataUpsertChatDestinationErrors, DataUpsertEmbeddingsData, DataUpsertEmbeddingsResponses, DataUpsertEmbeddingsErrors, DataGetTranslationData, DataGetTranslationResponses, DataGetTranslationErrors, DataListTranslationsData, DataListTranslationsResponses, DataListTranslationsErrors, DataUpsertTranslationsData, DataUpsertTranslationsResponses, DataUpsertTranslationsErrors, DataListLogsRouteData, DataListLogsRouteResponses, DataListLogsRouteErrors, DataCreateLogsRouteData, DataCreateLogsRouteResponses, DataCreateLogsRouteErrors, DataGetLogRouteData, DataGetLogRouteResponses, DataGetLogRouteErrors, DataPurgeLogsData, DataPurgeLogsResponses, DataPurgeLogsErrors, DataDbStatsData, DataDbStatsResponses, DataTableSizesData, DataTableSizesResponses, DataClearTableRouteData, DataClearTableRouteResponses, DataClearTableRouteErrors, DataGetNetworkSettingsData, DataGetNetworkSettingsResponses, DataPutNetworkSettingsData, DataPutNetworkSettingsResponses, DataPutNetworkSettingsErrors, DataGetSettingData, DataGetSettingResponses, DataGetSettingErrors, DataPutSettingData, DataPutSettingResponses, DataPutSettingErrors, DataImportDataData, DataImportDataResponses, DataImportDataErrors, DataExportDataData, DataExportDataResponses, DataExportDataErrors, RagRagStatusData, RagRagStatusResponses, RagRagEmbedData, RagRagEmbedResponses, RagRagEmbedErrors, RagRagSearchData, RagRagSearchResponses, RagRagSearchErrors, JobsJobsStatusData, JobsJobsStatusResponses, JobsGetRuntimeConfigData, JobsGetRuntimeConfigResponses, JobsTriggerSchedulerJobData, JobsTriggerSchedulerJobResponses, JobsTriggerSchedulerJobErrors, JobsUpdateSchedulerJobData, JobsUpdateSchedulerJobResponses, JobsUpdateSchedulerJobErrors, JobsListSyncLanesData, JobsListSyncLanesResponses, JobsPauseSyncLaneData, JobsPauseSyncLaneResponses, JobsPauseSyncLaneErrors, JobsResumeSyncLaneData, JobsResumeSyncLaneResponses, JobsResumeSyncLaneErrors, JobsDrainSyncLaneData, JobsDrainSyncLaneResponses, JobsDrainSyncLaneErrors, JobsStartSyncJobData, JobsStartSyncJobResponses, JobsStartSyncJobErrors, JobsGetSyncJobStatusData, JobsGetSyncJobStatusResponses, JobsGetSyncJobStatusErrors, JobsSyncJobEventsData, JobsSyncJobEventsResponses, JobsSyncJobEventsErrors, JobsCancelSyncJobData, JobsCancelSyncJobResponses, JobsCancelSyncJobErrors, QuotaReadQuotaUsageData, QuotaReadQuotaUsageResponses, QuotaReadQuotaUsageErrors, QuotaReadMyQuotaData, QuotaReadMyQuotaResponses, QuotaReadQuotaLimitsData, QuotaReadQuotaLimitsResponses, QuotaSetQuotaDefaultsData, QuotaSetQuotaDefaultsResponses, QuotaSetQuotaDefaultsErrors, QuotaSetQuotaLimitsForUserData, QuotaSetQuotaLimitsForUserResponses, QuotaSetQuotaLimitsForUserErrors, QuotaLiftQuotaCeilingData, QuotaLiftQuotaCeilingResponses, QuotaLiftQuotaCeilingErrors, ViewAsStartViewAsData, ViewAsStartViewAsResponses, ViewAsStartViewAsErrors, ViewAsElevateViewAsData, ViewAsElevateViewAsResponses, ViewAsElevateViewAsErrors, ViewAsReadViewAsSessionsData, ViewAsReadViewAsSessionsResponses, ViewAsReadViewAsSessionsErrors } from './types.gen';
import { client as _heyApiClient } from './client.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = ClientOptions<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Login Access Token
 * OAuth2 compatible token login, get an access token for future requests
 */
export const loginLoginAccessToken = <ThrowOnError extends boolean = true>(options: Options<LoginLoginAccessTokenData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<LoginLoginAccessTokenResponses, LoginLoginAccessTokenErrors, ThrowOnError, 'data'>({
        ...urlSearchParamsBodySerializer,
        responseStyle: 'data',
        url: '/api/v1/login/access-token',
        ...options,
        headers: {
            'Content-Type': 'application/x-www-form-urlencoded',
            ...options.headers
        }
    });
};

/**
 * Test Token
 * Test access token
 */
export const loginTestToken = <ThrowOnError extends boolean = true>(options?: Options<LoginTestTokenData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).post<LoginTestTokenResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/login/test-token',
        ...options
    });
};

/**
 * Recover Password
 * Request a password reset link.
 */
export const loginRecoverPassword = <ThrowOnError extends boolean = true>(options: Options<LoginRecoverPasswordData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<LoginRecoverPasswordResponses, LoginRecoverPasswordErrors, ThrowOnError, 'data'>({
        responseStyle: 'data',
        url: '/api/v1/password-recovery/{email}',
        ...options
    });
};

/**
 * Reset Password
 * Reset password
 */
export const loginResetPassword = <ThrowOnError extends boolean = true>(options: Options<LoginResetPasswordData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<LoginResetPasswordResponses, LoginResetPasswordErrors, ThrowOnError, 'data'>({
        responseStyle: 'data',
        url: '/api/v1/reset-password/',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Recover Password Html Content
 * HTML Content for Password Recovery
 */
export const loginRecoverPasswordHtmlContent = <ThrowOnError extends boolean = true>(options: Options<LoginRecoverPasswordHtmlContentData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<LoginRecoverPasswordHtmlContentResponses, LoginRecoverPasswordHtmlContentErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/password-recovery-html-content/{email}',
        ...options
    });
};

/**
 * Read Users
 * Retrieve users.
 */
export const usersReadUsers = <ThrowOnError extends boolean = true>(options?: Options<UsersReadUsersData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<UsersReadUsersResponses, UsersReadUsersErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/users/',
        ...options
    });
};

/**
 * Create User
 * Create new user.
 */
export const usersCreateUser = <ThrowOnError extends boolean = true>(options: Options<UsersCreateUserData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<UsersCreateUserResponses, UsersCreateUserErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/users/',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Delete User Me
 * Delete own user.
 */
export const usersDeleteUserMe = <ThrowOnError extends boolean = true>(options?: Options<UsersDeleteUserMeData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).delete<UsersDeleteUserMeResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/users/me',
        ...options
    });
};

/**
 * Read User Me
 * Get current user.
 */
export const usersReadUserMe = <ThrowOnError extends boolean = true>(options?: Options<UsersReadUserMeData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<UsersReadUserMeResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/users/me',
        ...options
    });
};

/**
 * Update User Me
 * Update own user.
 */
export const usersUpdateUserMe = <ThrowOnError extends boolean = true>(options: Options<UsersUpdateUserMeData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UsersUpdateUserMeResponses, UsersUpdateUserMeErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/users/me',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Update Password Me
 * Update own password.
 */
export const usersUpdatePasswordMe = <ThrowOnError extends boolean = true>(options: Options<UsersUpdatePasswordMeData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UsersUpdatePasswordMeResponses, UsersUpdatePasswordMeErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/users/me/password',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Register User
 * Create new user without the need to be logged in.
 */
export const usersRegisterUser = <ThrowOnError extends boolean = true>(options: Options<UsersRegisterUserData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<UsersRegisterUserResponses, UsersRegisterUserErrors, ThrowOnError, 'data'>({
        responseStyle: 'data',
        url: '/api/v1/users/signup',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Delete User
 * Delete a user.
 */
export const usersDeleteUser = <ThrowOnError extends boolean = true>(options: Options<UsersDeleteUserData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<UsersDeleteUserResponses, UsersDeleteUserErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/users/{user_id}',
        ...options
    });
};

/**
 * Read User By Id
 * Get a specific user by id.
 */
export const usersReadUserById = <ThrowOnError extends boolean = true>(options: Options<UsersReadUserByIdData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<UsersReadUserByIdResponses, UsersReadUserByIdErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/users/{user_id}',
        ...options
    });
};

/**
 * Update User
 * Update a user.
 */
export const usersUpdateUser = <ThrowOnError extends boolean = true>(options: Options<UsersUpdateUserData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UsersUpdateUserResponses, UsersUpdateUserErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/users/{user_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Test Email
 * Test emails.
 */
export const utilsTestEmail = <ThrowOnError extends boolean = true>(options: Options<UtilsTestEmailData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<UtilsTestEmailResponses, UtilsTestEmailErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/utils/test-email/',
        ...options
    });
};

/**
 * Health Check
 */
export const utilsHealthCheck = <ThrowOnError extends boolean = true>(options?: Options<UtilsHealthCheckData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<UtilsHealthCheckResponses, unknown, ThrowOnError, 'data'>({
        responseStyle: 'data',
        url: '/api/v1/utils/health-check/',
        ...options
    });
};

/**
 * Api Test Proxy
 */
export const networkApiTestProxy = <ThrowOnError extends boolean = true>(options: Options<NetworkApiTestProxyData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<NetworkApiTestProxyResponses, NetworkApiTestProxyErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/network/test-proxy',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Api Proxy Health
 */
export const networkApiProxyHealth = <ThrowOnError extends boolean = true>(options?: Options<NetworkApiProxyHealthData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<NetworkApiProxyHealthResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/network/proxy-health',
        ...options
    });
};

/**
 * Api Tor Status
 */
export const networkApiTorStatus = <ThrowOnError extends boolean = true>(options?: Options<NetworkApiTorStatusData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<NetworkApiTorStatusResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/network/tor-status',
        ...options
    });
};

/**
 * Api Tor Ip
 */
export const networkApiTorIp = <ThrowOnError extends boolean = true>(options?: Options<NetworkApiTorIpData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<NetworkApiTorIpResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/network/tor-ip',
        ...options
    });
};

/**
 * Api Tor Restart
 */
export const networkApiTorRestart = <ThrowOnError extends boolean = true>(options?: Options<NetworkApiTorRestartData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).post<NetworkApiTorRestartResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/network/tor-restart',
        ...options
    });
};

/**
 * Api Tor New Identity
 */
export const networkApiTorNewIdentity = <ThrowOnError extends boolean = true>(options: Options<NetworkApiTorNewIdentityData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<NetworkApiTorNewIdentityResponses, NetworkApiTorNewIdentityErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/network/tor-new-identity',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Api Scrape
 */
export const telegramApiScrape = <ThrowOnError extends boolean = true>(options: Options<TelegramApiScrapeData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<TelegramApiScrapeResponses, TelegramApiScrapeErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/telegram/scrape',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Api Channel Info
 */
export const telegramApiChannelInfo = <ThrowOnError extends boolean = true>(options: Options<TelegramApiChannelInfoData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<TelegramApiChannelInfoResponses, TelegramApiChannelInfoErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/telegram/channel-info',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Api Resolve Start Time
 */
export const telegramApiResolveStartTime = <ThrowOnError extends boolean = true>(options: Options<TelegramApiResolveStartTimeData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<TelegramApiResolveStartTimeResponses, TelegramApiResolveStartTimeErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/telegram/resolve-start-time',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Api Bot Info
 */
export const telegramApiBotInfo = <ThrowOnError extends boolean = true>(options: Options<TelegramApiBotInfoData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<TelegramApiBotInfoResponses, TelegramApiBotInfoErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/telegram/bot-info',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Api Publish
 */
export const telegramApiPublish = <ThrowOnError extends boolean = true>(options: Options<TelegramApiPublishData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<TelegramApiPublishResponses, TelegramApiPublishErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/telegram/publish',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Api Channel Photo
 */
export const telegramApiChannelPhoto = <ThrowOnError extends boolean = true>(options: Options<TelegramApiChannelPhotoData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<TelegramApiChannelPhotoResponses, TelegramApiChannelPhotoErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/telegram/channel-photo/{channel_id}',
        ...options
    });
};

/**
 * Api Post Thumb
 */
export const telegramApiPostThumb = <ThrowOnError extends boolean = true>(options: Options<TelegramApiPostThumbData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<TelegramApiPostThumbResponses, TelegramApiPostThumbErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/telegram/post-thumb/{channel_name}/{post_id}',
        ...options
    });
};

/**
 * Api Bot File
 */
export const telegramApiBotFile = <ThrowOnError extends boolean = true>(options: Options<TelegramApiBotFileData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<TelegramApiBotFileResponses, TelegramApiBotFileErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/telegram/bot-file/{credential_id}',
        ...options
    });
};

/**
 * Api List Models
 */
export const aiApiListModels = <ThrowOnError extends boolean = true>(options?: Options<AiApiListModelsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<AiApiListModelsResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/ai/models',
        ...options
    });
};

/**
 * Api Summary
 */
export const aiApiSummary = <ThrowOnError extends boolean = true>(options: Options<AiApiSummaryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AiApiSummaryResponses, AiApiSummaryErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/ai/summary',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Api Summary Prompt
 */
export const aiApiSummaryPrompt = <ThrowOnError extends boolean = true>(options: Options<AiApiSummaryPromptData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AiApiSummaryPromptResponses, AiApiSummaryPromptErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/ai/summary/prompt',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Api Summary Stream
 */
export const aiApiSummaryStream = <ThrowOnError extends boolean = true>(options: Options<AiApiSummaryStreamData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AiApiSummaryStreamResponses, AiApiSummaryStreamErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/ai/summary/stream',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Api Chat Stream
 */
export const aiApiChatStream = <ThrowOnError extends boolean = true>(options: Options<AiApiChatStreamData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AiApiChatStreamResponses, AiApiChatStreamErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/ai/chat/stream',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Api Tag Prompt
 */
export const aiApiTagPrompt = <ThrowOnError extends boolean = true>(options: Options<AiApiTagPromptData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AiApiTagPromptResponses, AiApiTagPromptErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/ai/tag/prompt',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Api Tag Stream
 */
export const aiApiTagStream = <ThrowOnError extends boolean = true>(options: Options<AiApiTagStreamData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AiApiTagStreamResponses, AiApiTagStreamErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/ai/tag/stream',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Api Embeddings
 */
export const aiApiEmbeddings = <ThrowOnError extends boolean = true>(options: Options<AiApiEmbeddingsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AiApiEmbeddingsResponses, AiApiEmbeddingsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/ai/embeddings',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Api Translate
 */
export const aiApiTranslate = <ThrowOnError extends boolean = true>(options: Options<AiApiTranslateData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AiApiTranslateResponses, AiApiTranslateErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/ai/translate',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Get Sync Meta Route
 */
export const dataGetSyncMetaRoute = <ThrowOnError extends boolean = true>(options?: Options<DataGetSyncMetaRouteData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataGetSyncMetaRouteResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/sync-meta',
        ...options
    });
};

/**
 * List Channels
 * The channel list, without `bio` — see `list_channel_bios`.
 *
 * Returns a hand-built `Response` so the body can be hashed into an `ETag`;
 * `response_model` above still drives the OpenAPI schema and the generated
 * client. `refetchOnWindowFocus` asks for this list on every window focus, and
 * channel rows are quiet minute to minute (0 changed in the last minute, 1 in
 * five, on staging), so most of those become a bodiless 304 instead of 494 KB.
 */
export const dataListChannels = <ThrowOnError extends boolean = true>(options?: Options<DataListChannelsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataListChannelsResponses, DataListChannelsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels',
        ...options
    });
};

/**
 * List Channel Stats
 * Post aggregates for every channel, keyed by channel name.
 *
 * The Channels tab's stats, split off `GET /channels?includeStats=true` so the
 * grid can paint without them: they cost 2.36s of a 3.13s response and 46 KB of
 * a 536 KB payload, and only two of the grid's eleven sort options read them.
 *
 * Declared ahead of every `/channels/{channel_id}` route so a literal "stats"
 * can never be captured as a channel id — the same ordering hazard the
 * `/discover/reports/latest` and `/settings/network` routes are placed against.
 */
export const dataListChannelStats = <ThrowOnError extends boolean = true>(options?: Options<DataListChannelStatsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataListChannelStatsResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels/stats',
        ...options
    });
};

/**
 * List Channel Bios
 * Every channel's bio, keyed by channel name.
 *
 * 40% of the channel list's gzipped bytes for something the grid clamps to two
 * lines on the ~20 cards on screen. Off the critical path here, the grid paints
 * from a 297 KB list instead of a 494 KB one.
 *
 * Same ordering placement as `/channels/stats`: ahead of `/channels/{id}`.
 */
export const dataListChannelBios = <ThrowOnError extends boolean = true>(options?: Options<DataListChannelBiosData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataListChannelBiosResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels/bios',
        ...options
    });
};

/**
 * Delete Channel
 * Remove the channel from the caller's list. Its posts are left alone.
 */
export const dataDeleteChannel = <ThrowOnError extends boolean = true>(options: Options<DataDeleteChannelData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DataDeleteChannelResponses, DataDeleteChannelErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels/{channel_id}',
        ...options
    });
};

/**
 * Upsert Channel
 * Edit a Channel, or follow one that already exists.
 *
 * A `PUT` from an account that does not follow this Channel yet creates the
 * follow; the Channel itself is shared corpus and is not re-created.
 */
export const dataUpsertChannel = <ThrowOnError extends boolean = true>(options: Options<DataUpsertChannelData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<DataUpsertChannelResponses, DataUpsertChannelErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels/{channel_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Start Bulk Follow
 */
export const dataStartBulkFollow = <ThrowOnError extends boolean = true>(options: Options<DataStartBulkFollowData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataStartBulkFollowResponses, DataStartBulkFollowErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels/bulk-follow',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Get Bulk Follow Status
 */
export const dataGetBulkFollowStatus = <ThrowOnError extends boolean = true>(options: Options<DataGetBulkFollowStatusData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<DataGetBulkFollowStatusResponses, DataGetBulkFollowStatusErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels/bulk-follow/{follow_job_id}',
        ...options
    });
};

/**
 * Bulk Follow Events
 */
export const dataBulkFollowEvents = <ThrowOnError extends boolean = true>(options: Options<DataBulkFollowEventsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<DataBulkFollowEventsResponses, DataBulkFollowEventsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels/bulk-follow/{follow_job_id}/events',
        ...options
    });
};

/**
 * Cancel Bulk Follow
 */
export const dataCancelBulkFollow = <ThrowOnError extends boolean = true>(options: Options<DataCancelBulkFollowData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataCancelBulkFollowResponses, DataCancelBulkFollowErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels/bulk-follow/{follow_job_id}/cancel',
        ...options
    });
};

/**
 * Bulk Reresolve Start Ids Endpoint
 */
export const dataBulkReresolveStartIdsEndpoint = <ThrowOnError extends boolean = true>(options?: Options<DataBulkReresolveStartIdsEndpointData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).post<DataBulkReresolveStartIdsEndpointResponses, DataBulkReresolveStartIdsEndpointErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels/bulk-reresolve-start-ids',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Bulk Reset Sync Endpoint
 */
export const dataBulkResetSyncEndpoint = <ThrowOnError extends boolean = true>(options: Options<DataBulkResetSyncEndpointData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataBulkResetSyncEndpointResponses, DataBulkResetSyncEndpointErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels/bulk-reset-sync',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Bulk Sync Settings Endpoint
 */
export const dataBulkSyncSettingsEndpoint = <ThrowOnError extends boolean = true>(options: Options<DataBulkSyncSettingsEndpointData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<DataBulkSyncSettingsEndpointResponses, DataBulkSyncSettingsEndpointErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels/bulk-sync-settings',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List Setting Groups
 */
export const dataListSettingGroups = <ThrowOnError extends boolean = true>(options?: Options<DataListSettingGroupsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataListSettingGroupsResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/setting-groups',
        ...options
    });
};

/**
 * Create Setting Group
 */
export const dataCreateSettingGroup = <ThrowOnError extends boolean = true>(options: Options<DataCreateSettingGroupData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataCreateSettingGroupResponses, DataCreateSettingGroupErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/setting-groups',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Delete Setting Group
 */
export const dataDeleteSettingGroup = <ThrowOnError extends boolean = true>(options: Options<DataDeleteSettingGroupData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DataDeleteSettingGroupResponses, DataDeleteSettingGroupErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/setting-groups/{group_id}',
        ...options
    });
};

/**
 * Update Setting Group
 */
export const dataUpdateSettingGroup = <ThrowOnError extends boolean = true>(options: Options<DataUpdateSettingGroupData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<DataUpdateSettingGroupResponses, DataUpdateSettingGroupErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/setting-groups/{group_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Bulk Assign Setting Group
 */
export const dataBulkAssignSettingGroup = <ThrowOnError extends boolean = true>(options: Options<DataBulkAssignSettingGroupData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<DataBulkAssignSettingGroupResponses, DataBulkAssignSettingGroupErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels/bulk-setting-group',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Bulk Channel Tags Endpoint
 */
export const dataBulkChannelTagsEndpoint = <ThrowOnError extends boolean = true>(options: Options<DataBulkChannelTagsEndpointData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<DataBulkChannelTagsEndpointResponses, DataBulkChannelTagsEndpointErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels/bulk-tags',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Get Channel Stats
 */
export const dataGetChannelStats = <ThrowOnError extends boolean = true>(options: Options<DataGetChannelStatsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<DataGetChannelStatsResponses, DataGetChannelStatsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/channels/{channel_id}/stats',
        ...options
    });
};

/**
 * List Posts
 * One page of posts for a channel/date scope.
 *
 * With no filters, no cap and ``sort=time`` this is the newest-first page the
 * export/lookup fallbacks and language detection rely on. The Posts feed also
 * passes keyword/forwarded/media filters, a per-channel cap, a sort order and
 * ``offset`` so the whole view is assembled server-side instead of paging a
 * channel's history into the browser.
 *
 * POST rather than GET because the scope carries the channel selection, which
 * can be the entire account — see `PostScopeRequest`. This is a read expressed
 * as a POST purely so the selection travels in the body.
 */
export const dataListPosts = <ThrowOnError extends boolean = true>(options: Options<DataListPostsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataListPostsResponses, DataListPostsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/posts',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Posts Counts
 * Per-channel post counts for a filtered scope, computed as a SQL GROUP BY.
 *
 * Replaces the client's `buildPostsInScopeCounts`, which counted the fully
 * fetched, client-filtered post array.
 *
 * POST rather than GET because the scope carries the channel selection: this is
 * a read expressed as a POST purely so the selection travels in the body.
 */
export const dataPostsCounts = <ThrowOnError extends boolean = true>(options: Options<DataPostsCountsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataPostsCountsResponses, DataPostsCountsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/posts/counts',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Lookup Posts Route
 */
export const dataLookupPostsRoute = <ThrowOnError extends boolean = true>(options: Options<DataLookupPostsRouteData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataLookupPostsRouteResponses, DataLookupPostsRouteErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/posts/lookup',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Bulk Upsert Posts Route
 */
export const dataBulkUpsertPostsRoute = <ThrowOnError extends boolean = true>(options: Options<DataBulkUpsertPostsRouteData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataBulkUpsertPostsRouteResponses, DataBulkUpsertPostsRouteErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/posts/bulk',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Discover Candidates
 * Aggregated discovery candidates for a channel/date scope.
 *
 * Returns counts only. The client previously fetched every post body in
 * scope to compute this in JS. The keyword/forwarded/media/cap params
 * reproduce the Posts-tab view the client aggregated over, and
 * `maxPerChannelMode`/`seed`/`postIds` cover the `random` cap and semantic
 * scopes that used to keep a second client-side implementation alive.
 *
 * POST rather than GET for the same reason as `/posts` — the channel selection
 * travels in the body so it cannot overflow the request line.
 */
export const dataDiscoverCandidates = <ThrowOnError extends boolean = true>(options: Options<DataDiscoverCandidatesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataDiscoverCandidatesResponses, DataDiscoverCandidatesErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/discover/candidates',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Remove Discover Ignored
 * Undo a dismissal.
 *
 * DELETE with a body rather than a path param so a batch can be undone in one
 * call, matching the POST.
 */
export const dataRemoveDiscoverIgnored = <ThrowOnError extends boolean = true>(options: Options<DataRemoveDiscoverIgnoredData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DataRemoveDiscoverIgnoredResponses, DataRemoveDiscoverIgnoredErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/discover/ignored',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List Discover Ignored
 * Dismissed candidates, newest first.
 */
export const dataListDiscoverIgnored = <ThrowOnError extends boolean = true>(options?: Options<DataListDiscoverIgnoredData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataListDiscoverIgnoredResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/discover/ignored',
        ...options
    });
};

/**
 * Add Discover Ignored
 * Dismiss candidates so later reports stop re-surfacing them.
 *
 * Idempotent: re-dismissing an entry is a no-op rather than an error, since
 * the UI treats this as a toggle.
 */
export const dataAddDiscoverIgnored = <ThrowOnError extends boolean = true>(options: Options<DataAddDiscoverIgnoredData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataAddDiscoverIgnoredResponses, DataAddDiscoverIgnoredErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/discover/ignored',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List Discover Probes
 * One page of cached handle probes, optionally filtered by status.
 */
export const dataListDiscoverProbes = <ThrowOnError extends boolean = true>(options?: Options<DataListDiscoverProbesData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataListDiscoverProbesResponses, DataListDiscoverProbesErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/discover/probes',
        ...options
    });
};

/**
 * Get Discover Probe Queue
 * Probe queue state, for the progress display.
 *
 * There is no job id to poll: probing is a scheduled backend job draining a
 * durable queue (`app.jobs.discover_probe`), not something a client starts.
 * Everything the UI needs is a count, and the verdicts themselves arrive
 * through the report read, which already joins the probe table.
 *
 * `enabled` reflects the operator's pause switch — the ordinary job toggle, so
 * pausing is durable and every open tab agrees about it.
 */
export const dataGetDiscoverProbeQueue = <ThrowOnError extends boolean = true>(options?: Options<DataGetDiscoverProbeQueueData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataGetDiscoverProbeQueueResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/discover/probe/queue',
        ...options
    });
};

/**
 * Recheck Discover Probes
 * Discard cached verdicts for these handles and put them back in the queue.
 *
 * The escape hatch for a verdict that is wrong or has gone stale: a private
 * channel that opened up, or a handle misjudged during an outage. Without it,
 * caching indefinitely would mean a single bad answer is permanent.
 *
 * Requeues at the front rather than merely forgetting. A row is both the cached
 * answer and the work item, so deleting it would drop the handle out of the
 * queue and nothing would fetch it again. The next drain tick picks these up
 * first, so the wait is bounded by the job interval.
 */
export const dataRecheckDiscoverProbes = <ThrowOnError extends boolean = true>(options: Options<DataRecheckDiscoverProbesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataRecheckDiscoverProbesResponses, DataRecheckDiscoverProbesErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/discover/probe/recheck',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List Discover Reports
 * Newest-first page of saved reports, without their candidate rows.
 *
 * See `report_to_camel_light`: a wide-scope report holds the full
 * single-reference tail, so the list ships a `candidateCount` instead.
 */
export const dataListDiscoverReports = <ThrowOnError extends boolean = true>(options?: Options<DataListDiscoverReportsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataListDiscoverReportsResponses, DataListDiscoverReportsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/discover/reports',
        ...options
    });
};

/**
 * Create Discover Report
 * Generate a Discover report and save it.
 *
 * Unlike `/discover/candidates`, which computes and forgets, this persists the
 * result together with a snapshot of the scope it was generated for. The saved
 * report is immutable: later changes to the channel selection or the Posts-tab
 * filters produce a *new* report rather than altering this one (IDEA-011 W1).
 */
export const dataCreateDiscoverReport = <ThrowOnError extends boolean = true>(options: Options<DataCreateDiscoverReportData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataCreateDiscoverReportResponses, DataCreateDiscoverReportErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/discover/reports',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Delete Discover Report
 */
export const dataDeleteDiscoverReport = <ThrowOnError extends boolean = true>(options: Options<DataDeleteDiscoverReportData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DataDeleteDiscoverReportResponses, DataDeleteDiscoverReportErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/discover/reports/{report_id}',
        ...options
    });
};

/**
 * Get Discover Report
 * A saved report with every candidate, `isFollowed` resolved live.
 */
export const dataGetDiscoverReport = <ThrowOnError extends boolean = true>(options: Options<DataGetDiscoverReportData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<DataGetDiscoverReportResponses, DataGetDiscoverReportErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/discover/reports/{report_id}',
        ...options
    });
};

/**
 * Update Discover Report Flags
 * Star or annotate a saved report — the only write it accepts.
 */
export const dataUpdateDiscoverReportFlags = <ThrowOnError extends boolean = true>(options: Options<DataUpdateDiscoverReportFlagsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<DataUpdateDiscoverReportFlagsResponses, DataUpdateDiscoverReportFlagsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/discover/reports/{report_id}/flags',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List Summaries
 * List in the light projection — see `summary_to_camel_light`.
 *
 * `search` matches channels/text/promptText/model/note in SQL, so prompt
 * bodies stay searchable without being shipped to the client.
 */
export const dataListSummaries = <ThrowOnError extends boolean = true>(options?: Options<DataListSummariesData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataListSummariesResponses, DataListSummariesErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/summaries',
        ...options
    });
};

/**
 * Delete Summary
 */
export const dataDeleteSummary = <ThrowOnError extends boolean = true>(options: Options<DataDeleteSummaryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DataDeleteSummaryResponses, DataDeleteSummaryErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/summaries/{summary_id}',
        ...options
    });
};

/**
 * Get Summary
 * Full summary including citedPosts/promptText/chatMessages.
 */
export const dataGetSummary = <ThrowOnError extends boolean = true>(options: Options<DataGetSummaryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<DataGetSummaryResponses, DataGetSummaryErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/summaries/{summary_id}',
        ...options
    });
};

/**
 * Upsert Summary
 */
export const dataUpsertSummary = <ThrowOnError extends boolean = true>(options: Options<DataUpsertSummaryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<DataUpsertSummaryResponses, DataUpsertSummaryErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/summaries/{summary_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List Tag Runs
 * List runs in the light projection — see `tag_run_to_camel_light`.
 */
export const dataListTagRuns = <ThrowOnError extends boolean = true>(options?: Options<DataListTagRunsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataListTagRunsResponses, DataListTagRunsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/tag-runs',
        ...options
    });
};

/**
 * Delete Tag Run
 */
export const dataDeleteTagRun = <ThrowOnError extends boolean = true>(options: Options<DataDeleteTagRunData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DataDeleteTagRunResponses, DataDeleteTagRunErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/tag-runs/{tag_run_id}',
        ...options
    });
};

/**
 * Get Tag Run
 * Full run including promptText/responseText/suggestions.
 */
export const dataGetTagRun = <ThrowOnError extends boolean = true>(options: Options<DataGetTagRunData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<DataGetTagRunResponses, DataGetTagRunErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/tag-runs/{tag_run_id}',
        ...options
    });
};

/**
 * Upsert Tag Run
 */
export const dataUpsertTagRun = <ThrowOnError extends boolean = true>(options: Options<DataUpsertTagRunData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<DataUpsertTagRunResponses, DataUpsertTagRunErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/tag-runs/{tag_run_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List Chat Sessions
 * List in the light projection — see `chat_session_to_camel_light`.
 *
 * `search` matches title/channels/model/note in SQL. It does not reach the
 * transcript; the title already carries the first user message.
 */
export const dataListChatSessions = <ThrowOnError extends boolean = true>(options?: Options<DataListChatSessionsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataListChatSessionsResponses, DataListChatSessionsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/chat-sessions',
        ...options
    });
};

/**
 * Delete Chat Session
 */
export const dataDeleteChatSession = <ThrowOnError extends boolean = true>(options: Options<DataDeleteChatSessionData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DataDeleteChatSessionResponses, DataDeleteChatSessionErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/chat-sessions/{chat_session_id}',
        ...options
    });
};

/**
 * Get Chat Session
 * Full chat session including the transcript.
 */
export const dataGetChatSession = <ThrowOnError extends boolean = true>(options: Options<DataGetChatSessionData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<DataGetChatSessionResponses, DataGetChatSessionErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/chat-sessions/{chat_session_id}',
        ...options
    });
};

/**
 * Upsert Chat Session
 */
export const dataUpsertChatSession = <ThrowOnError extends boolean = true>(options: Options<DataUpsertChatSessionData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<DataUpsertChatSessionResponses, DataUpsertChatSessionErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/chat-sessions/{chat_session_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List Artifacts
 * One newest-first page of every saved artifact, whatever its kind.
 *
 * A `UNION ALL` over four tables projecting **named columns only**. It must not
 * open `tg_summary_payloads` or `tg_chat_session_payloads`, and must not select
 * the corpus-sized columns of `tg_tag_runs` / `tg_discover_reports` — pinned by
 * `tests/services/test_artifact_list_payload_cost.py`.
 *
 * `search` deliberately does not reach summary prompt bodies; that would mean
 * opening a payload table. `/data/summaries?search=` still does.
 */
export const dataListArtifacts = <ThrowOnError extends boolean = true>(options?: Options<DataListArtifactsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataListArtifactsResponses, DataListArtifactsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/artifacts',
        ...options
    });
};

/**
 * List Bot Credentials
 */
export const dataListBotCredentials = <ThrowOnError extends boolean = true>(options?: Options<DataListBotCredentialsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataListBotCredentialsResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/bot-credentials',
        ...options
    });
};

/**
 * Delete Bot Credential
 */
export const dataDeleteBotCredential = <ThrowOnError extends boolean = true>(options: Options<DataDeleteBotCredentialData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DataDeleteBotCredentialResponses, DataDeleteBotCredentialErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/bot-credentials/{bot_id}',
        ...options
    });
};

/**
 * Upsert Bot Credential
 */
export const dataUpsertBotCredential = <ThrowOnError extends boolean = true>(options: Options<DataUpsertBotCredentialData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<DataUpsertBotCredentialResponses, DataUpsertBotCredentialErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/bot-credentials/{bot_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Migrate Bot Credentials
 */
export const dataMigrateBotCredentials = <ThrowOnError extends boolean = true>(options: Options<DataMigrateBotCredentialsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataMigrateBotCredentialsResponses, DataMigrateBotCredentialsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/bot-credentials/migrate',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List Chat Destinations
 */
export const dataListChatDestinations = <ThrowOnError extends boolean = true>(options?: Options<DataListChatDestinationsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataListChatDestinationsResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/chat-destinations',
        ...options
    });
};

/**
 * Delete Chat Destination
 */
export const dataDeleteChatDestination = <ThrowOnError extends boolean = true>(options: Options<DataDeleteChatDestinationData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DataDeleteChatDestinationResponses, DataDeleteChatDestinationErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/chat-destinations/{dest_id}',
        ...options
    });
};

/**
 * Upsert Chat Destination
 */
export const dataUpsertChatDestination = <ThrowOnError extends boolean = true>(options: Options<DataUpsertChatDestinationData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<DataUpsertChatDestinationResponses, DataUpsertChatDestinationErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/chat-destinations/{dest_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Upsert Embeddings
 */
export const dataUpsertEmbeddings = <ThrowOnError extends boolean = true>(options: Options<DataUpsertEmbeddingsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataUpsertEmbeddingsResponses, DataUpsertEmbeddingsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/embeddings',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Get Translation
 * Read a single translation. Returns null when absent.
 */
export const dataGetTranslation = <ThrowOnError extends boolean = true>(options: Options<DataGetTranslationData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<DataGetTranslationResponses, DataGetTranslationErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/translations/one',
        ...options
    });
};

/**
 * List Translations
 */
export const dataListTranslations = <ThrowOnError extends boolean = true>(options?: Options<DataListTranslationsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataListTranslationsResponses, DataListTranslationsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/translations',
        ...options
    });
};

/**
 * Upsert Translations
 */
export const dataUpsertTranslations = <ThrowOnError extends boolean = true>(options: Options<DataUpsertTranslationsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataUpsertTranslationsResponses, DataUpsertTranslationsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/translations',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List Logs Route
 * One newest-first page of any log type, without the corpus-sized fields.
 *
 * Paged rather than whole-table: these tables carry request/response JSON and
 * grow without bound between retention sweeps, so an unfiltered select can
 * materialise gigabytes and OOM the worker. Full data ships via the export
 * path, which streams.
 *
 * The bodies are on `GET /logs/{log_type}/{log_id}` — this page was 56.28 MB
 * for 500 rows, 99.7% of it bodies the viewer only renders for an expanded
 * row. `search` runs in SQL so they stay findable without being sent, and
 * `searchInDetails` extends the match into them; both now search the whole
 * table rather than the page that happened to be fetched.
 */
export const dataListLogsRoute = <ThrowOnError extends boolean = true>(options: Options<DataListLogsRouteData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<DataListLogsRouteResponses, DataListLogsRouteErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/logs/{log_type}',
        ...options
    });
};

/**
 * Create Logs Route
 * Upsert a batch of log rows of one type.
 */
export const dataCreateLogsRoute = <ThrowOnError extends boolean = true>(options: Options<DataCreateLogsRouteData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataCreateLogsRouteResponses, DataCreateLogsRouteErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/logs/{log_type}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Get Log Route
 * One log row in full, bodies included.
 *
 * The other half of the list projection. `GET /data/logs/sync` shipped
 * 56.28 MB for a page of 500 rows, 99.7% of it request/response bodies that
 * the viewer renders only for an expanded row — and it expands one at a time.
 * This serves that one row.
 */
export const dataGetLogRoute = <ThrowOnError extends boolean = true>(options: Options<DataGetLogRouteData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<DataGetLogRouteResponses, DataGetLogRouteErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/logs/{log_type}/{log_id}',
        ...options
    });
};

/**
 * Purge Logs
 * Three deletes behind one endpoint, and they are not the same act.
 *
 * `olderThanDays` and `clearAll` sweep across every account, so both demand
 * `DATA_ADMIN`. `logId` removes exactly one row, which is what the delete
 * button on each of the five Logs tabs calls, for everybody — gating the whole
 * route turned that into an error toast for any non-Admin, so the gate is per
 * branch. The single-row branch answers to the owner instead.
 *
 * `SHARED_LOG_TYPES` is the exception, and it is derived from the tenancy
 * classification rather than listed here: for a type nobody owns a row of,
 * there is no owner for `get_log` to answer to, so the single-row delete is an
 * administrative act like the two sweeps. That covers sync logs from ticket 19
 * and network logs, which have been deletable one row at a time by any
 * authenticated account since the Admin gate went on their *reads* alone.
 */
export const dataPurgeLogs = <ThrowOnError extends boolean = true>(options?: Options<DataPurgeLogsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).delete<DataPurgeLogsResponses, DataPurgeLogsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/logs',
        ...options
    });
};

/**
 * Db Stats
 */
export const dataDbStats = <ThrowOnError extends boolean = true>(options?: Options<DataDbStatsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataDbStatsResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/stats',
        ...options
    });
};

/**
 * Table Sizes
 */
export const dataTableSizes = <ThrowOnError extends boolean = true>(options?: Options<DataTableSizesData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataTableSizesResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/table-sizes',
        ...options
    });
};

/**
 * Clear Table Route
 */
export const dataClearTableRoute = <ThrowOnError extends boolean = true>(options: Options<DataClearTableRouteData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DataClearTableRouteResponses, DataClearTableRouteErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/tables/{name}',
        ...options
    });
};

/**
 * Get Network Settings
 */
export const dataGetNetworkSettings = <ThrowOnError extends boolean = true>(options?: Options<DataGetNetworkSettingsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataGetNetworkSettingsResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/settings/network',
        ...options
    });
};

/**
 * Put Network Settings
 */
export const dataPutNetworkSettings = <ThrowOnError extends boolean = true>(options: Options<DataPutNetworkSettingsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<DataPutNetworkSettingsResponses, DataPutNetworkSettingsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/settings/network',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Get Setting
 */
export const dataGetSetting = <ThrowOnError extends boolean = true>(options: Options<DataGetSettingData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<DataGetSettingResponses, DataGetSettingErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/settings/{key}',
        ...options
    });
};

/**
 * Put Setting
 * Write one settings section, refusing deployment policy to a non-Admin.
 *
 * The route stays open because the *key* decides, not the path. `jobs` turns
 * the scheduler off for everybody, and so on for every other global key:
 * those are `DATA_ADMIN`.
 *
 * `sync` and `retention` cannot be gated that way, because each is a facade:
 * one body carries deployment policy and the caller's own preferences at the
 * same time. `sync` mixes scheduler policy, the runtime counters the Pause
 * button writes, and a person's start-time defaults; `retention` mixes the
 * corpus window — `postRetentionDays`, which deletes every account's Posts on
 * the next sweep, table clearing on a timer — with that person's own log and
 * report windows. Refusing the whole request would take their own settings
 * away from them, so for a caller without the permission the body is narrowed
 * to the fields the registry declares personal and the rest is dropped rather
 * than written. Dropping, not refusing, because the frontend sends a whole
 * section at once and a non-Admin saving their preferences should not be told
 * the save failed when the half that is theirs succeeded.
 */
export const dataPutSetting = <ThrowOnError extends boolean = true>(options: Options<DataPutSettingData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<DataPutSettingResponses, DataPutSettingErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/settings/{key}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Import Data
 * Restore a document into one account's rows.
 *
 * `subject` is the account the document lands under, defaulting to the caller
 * — the same parameter the export takes, so a backup of one person restores
 * as that person instead of as whoever ran it (ticket 28). `all` is refused:
 * an import writes rows and a document carries no owners, so "everybody" has
 * no meaning here that is not "the caller", which is what leaving it off
 * already says.
 *
 * Importing *for* somebody is a write on their behalf, so the acting Owner is
 * bound for this session before anything is written and every artifact the
 * document restores records who really uploaded it (ticket 27).
 */
export const dataImportData = <ThrowOnError extends boolean = true>(options: Options<DataImportDataData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DataImportDataResponses, DataImportDataErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/import',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Export Data
 * One account's export, or the whole deployment's — never truncated.
 *
 * `subject` is ticket 28: absent for the caller's own rows, a user id for that
 * account's, `all` for everybody's.
 *
 * Streamed rather than built in memory: the payload spans every post and log
 * row, which is far more than a worker can hold at once. The per-section row
 * counts are computed first and travel in `X-Export-Rows`, because a
 * `StreamingResponse` sends its headers before the generator runs — which is
 * what makes "reports the row count before starting" true for a client that
 * has not parsed a byte of the body yet. The same numbers lead the document,
 * from the same computation, so the header and the file cannot disagree.
 */
export const dataExportData = <ThrowOnError extends boolean = true>(options?: Options<DataExportDataData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<DataExportDataResponses, DataExportDataErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/data/export',
        ...options
    });
};

/**
 * Rag Status
 */
export const ragRagStatus = <ThrowOnError extends boolean = true>(options?: Options<RagRagStatusData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<RagRagStatusResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/rag/status',
        ...options
    });
};

/**
 * Rag Embed
 */
export const ragRagEmbed = <ThrowOnError extends boolean = true>(options: Options<RagRagEmbedData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<RagRagEmbedResponses, RagRagEmbedErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/rag/embed',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Rag Search
 */
export const ragRagSearch = <ThrowOnError extends boolean = true>(options: Options<RagRagSearchData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<RagRagSearchResponses, RagRagSearchErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/rag/search',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Jobs Status
 */
export const jobsJobsStatus = <ThrowOnError extends boolean = true>(options?: Options<JobsJobsStatusData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<JobsJobsStatusResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/jobs/status',
        ...options
    });
};

/**
 * Get Runtime Config
 */
export const jobsGetRuntimeConfig = <ThrowOnError extends boolean = true>(options?: Options<JobsGetRuntimeConfigData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<JobsGetRuntimeConfigResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/jobs/runtime-config',
        ...options
    });
};

/**
 * Trigger Scheduler Job
 */
export const jobsTriggerSchedulerJob = <ThrowOnError extends boolean = true>(options: Options<JobsTriggerSchedulerJobData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<JobsTriggerSchedulerJobResponses, JobsTriggerSchedulerJobErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/jobs/{job_id}/trigger',
        ...options
    });
};

/**
 * Update Scheduler Job
 */
export const jobsUpdateSchedulerJob = <ThrowOnError extends boolean = true>(options: Options<JobsUpdateSchedulerJobData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<JobsUpdateSchedulerJobResponses, JobsUpdateSchedulerJobErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/jobs/{job_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List Sync Lanes
 * The six lanes, in drain order, with their depth and paused state.
 */
export const jobsListSyncLanes = <ThrowOnError extends boolean = true>(options?: Options<JobsListSyncLanesData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<JobsListSyncLanesResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/jobs/lanes',
        ...options
    });
};

/**
 * Pause Sync Lane
 * Stop the worker taking new messages from this lane.
 *
 * Reversible and lossless: the messages stay queued and visible, and the next
 * drain after a resume picks them up. That is the difference from
 * `drain_sync_lane` below, which is neither.
 */
export const jobsPauseSyncLane = <ThrowOnError extends boolean = true>(options: Options<JobsPauseSyncLaneData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<JobsPauseSyncLaneResponses, JobsPauseSyncLaneErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/jobs/lanes/{lane}/pause',
        ...options
    });
};

/**
 * Resume Sync Lane
 */
export const jobsResumeSyncLane = <ThrowOnError extends boolean = true>(options: Options<JobsResumeSyncLaneData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<JobsResumeSyncLaneResponses, JobsResumeSyncLaneErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/jobs/lanes/{lane}/resume',
        ...options
    });
};

/**
 * Drain Sync Lane
 * Empty this lane now: archive every message and cancel the jobs behind it.
 *
 * Destructive, and gated on `JOBS_MANAGE` for the reason that permission
 * already gives for covering `POST /{job_id}/trigger`: triggering retention
 * deletes Posts, so this permission is not a read-only one and pausing or
 * discarding queued work is the same audience and the same act.
 */
export const jobsDrainSyncLane = <ThrowOnError extends boolean = true>(options: Options<JobsDrainSyncLaneData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<JobsDrainSyncLaneResponses, JobsDrainSyncLaneErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/jobs/lanes/{lane}/drain',
        ...options
    });
};

/**
 * Start Sync Job
 */
export const jobsStartSyncJob = <ThrowOnError extends boolean = true>(options: Options<JobsStartSyncJobData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<JobsStartSyncJobResponses, JobsStartSyncJobErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/jobs/sync',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Get Sync Job Status
 */
export const jobsGetSyncJobStatus = <ThrowOnError extends boolean = true>(options: Options<JobsGetSyncJobStatusData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<JobsGetSyncJobStatusResponses, JobsGetSyncJobStatusErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/jobs/sync/{job_id}',
        ...options
    });
};

/**
 * Sync Job Events
 */
export const jobsSyncJobEvents = <ThrowOnError extends boolean = true>(options: Options<JobsSyncJobEventsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<JobsSyncJobEventsResponses, JobsSyncJobEventsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/jobs/sync/{job_id}/events',
        ...options
    });
};

/**
 * Cancel Sync Job
 */
export const jobsCancelSyncJob = <ThrowOnError extends boolean = true>(options: Options<JobsCancelSyncJobData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<JobsCancelSyncJobResponses, JobsCancelSyncJobErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/jobs/sync/{job_id}/cancel',
        ...options
    });
};

/**
 * Read Quota Usage
 * Every account's Request usage on one UTC day, per Budget.
 *
 * `day` defaults to today. Absent accounts spent nothing — the ledger only
 * holds rows for work that actually happened, so a quiet day is a short list
 * rather than a page of zeros.
 */
export const quotaReadQuotaUsage = <ThrowOnError extends boolean = true>(options?: Options<QuotaReadQuotaUsageData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<QuotaReadQuotaUsageResponses, QuotaReadQuotaUsageErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/quota/usage',
        ...options
    });
};

/**
 * Read My Quota
 * Where the calling account stands on each Budget today (ticket 24).
 *
 * The fourth checkbox: a User sees per-Budget usage, and the browser turns
 * `status` into the persistent warning. `status` is computed here rather than
 * in the browser because the derivation is three comparisons in which zero
 * means opposite things on the two rungs, and getting it wrong client-side
 * would show "blocked" to somebody whose work is running.
 */
export const quotaReadMyQuota = <ThrowOnError extends boolean = true>(options?: Options<QuotaReadMyQuotaData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<QuotaReadMyQuotaResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/quota/me',
        ...options
    });
};

/**
 * Read Quota Limits
 * The deployment's defaults and every per-account override.
 *
 * Both in one response: an override field rendered blank has to say which
 * number it inherits, so a screen that fetched them separately would be
 * briefly and visibly wrong.
 *
 * **Both the resolved defaults and the stored ones**, which is not
 * redundancy. The resolved pair is what is in force, `config.py` fallback
 * included, and is what a form should show as *placeholder* text. The stored
 * pair is what an Admin has actually saved, `null` where they never have, and
 * is what the form's input *value* must be. Sending only the resolved numbers
 * makes a form seed every box with the shipped value, so the first save writes
 * all six into the settings row and `QUOTA_DEFAULT_*` in `.env` goes silently
 * dead for that deployment — and "leave it empty to inherit" stops being
 * sayable, because no box is ever empty.
 */
export const quotaReadQuotaLimits = <ThrowOnError extends boolean = true>(options?: Options<QuotaReadQuotaLimitsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<QuotaReadQuotaLimitsResponses, unknown, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/quota/limits',
        ...options
    });
};

/**
 * Set Quota Defaults
 * Set the deployment-wide default allowance and ceiling per Budget.
 *
 * Written to the global `quota` settings row through `settings_store`, which
 * is the sole writer of `tg_app_settings` — a second writer here would be the
 * second opinion about where a key belongs that the ticket 06 split exists to
 * remove.
 *
 * A `null` field clears that default, dropping back to `config.py`. It is
 * stored as a null rather than deleted, which `stored_default` reads as
 * absent either way.
 */
export const quotaSetQuotaDefaults = <ThrowOnError extends boolean = true>(options: Options<QuotaSetQuotaDefaultsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<QuotaSetQuotaDefaultsResponses, QuotaSetQuotaDefaultsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/quota/limits/defaults',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Set Quota Limits For User
 * Override one account's allowance and ceiling on the Budgets named.
 *
 * A Budget the body omits keeps whatever override it had, so a screen editing
 * one row does not have to echo the other two back to preserve them. Sending
 * both numbers as `null` removes the override entirely, which is the only way
 * to put an account back on the deployment default.
 */
export const quotaSetQuotaLimitsForUser = <ThrowOnError extends boolean = true>(options: Options<QuotaSetQuotaLimitsForUserData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<QuotaSetQuotaLimitsForUserResponses, QuotaSetQuotaLimitsForUserErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/quota/limits/{user_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Lift Quota Ceiling
 * Stop enforcing this account's ceilings for the current UTC day.
 *
 * Decision 18's "an Admin can lift early". The auto-lift needs no route: the
 * ledger is keyed by day, so a ceiling stops applying at the same UTC midnight
 * the spend resets at.
 *
 * An empty `budgets` lifts all three, which is what an Admin unblocking
 * somebody in a hurry means. `lifted: false` takes it back.
 */
export const quotaLiftQuotaCeiling = <ThrowOnError extends boolean = true>(options: Options<QuotaLiftQuotaCeilingData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<QuotaLiftQuotaCeilingResponses, QuotaLiftQuotaCeilingErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/quota/lifts/{user_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Start View As
 * Exchange the caller's token for a read-only look at one account.
 *
 * Refused for a target that holds `VIEW_AS` themselves — the ticket's "viewing
 * as another holder of the permission is refused", and the reason is that a
 * peer's account is the one place the permission could be used to acquire more
 * of itself. Refused for the caller's own account too: a View-as session that
 * is read-only over your *own* data is a downgrade nobody asked for, and it
 * would be indistinguishable in the audit trail from the real thing.
 *
 * Both refusals, and "no such account", answer with **one message**. A caller
 * holding `VIEW_AS` also holds `USERS_READ` and can already list every
 * account, so there is no enumeration to protect — but which accounts hold
 * which permissions is a different fact, and this route is not the place to
 * publish it.
 *
 * **Nesting needs no check of its own here, and adding one would be a guard
 * that cannot fail.** This is a POST, and `deps.view_as_allows` does not
 * allowlist it, so a View-as token is refused by `get_current_user` before the
 * handler runs. `test_view_as.py` asserts that at the HTTP level rather than
 * here — which is the assertion that survives ticket 27 widening what an
 * elevated session may do, where an unreachable branch in this file would
 * silently become the only thing standing between an audit row and a lie.
 */
export const viewAsStartViewAs = <ThrowOnError extends boolean = true>(options: Options<ViewAsStartViewAsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<ViewAsStartViewAsResponses, ViewAsStartViewAsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/view-as/{user_id}',
        ...options
    });
};

/**
 * Elevate View As
 * Exchange the Owner's token for a short session that may *write* (27).
 *
 * **A second exchange, not a flag flipped on the first.** It is authorised by
 * the Owner's own token exactly as `start_view_as` is, which is what makes
 * self-escalation impossible without a check of its own: `get_current_user`
 * refuses every POST from an `act`-bearing token, so a live session cannot
 * reach this route at all, whichever mode it is in. `deps` refuses the whole
 * `/view-as` family for an elevated session on top of that, and
 * `test_view_as_elevation.py` asserts both — the belt is structural and the
 * braces are the inventory.
 *
 * **Refused for a target holding any permission at all.** The ticket says
 * "refused when the target is an Admin" and `role == "admin"` is the spelling
 * `CLAUDE.md` forbids: a fourth privileged role added as a row walks straight
 * past it. The seeded `user` role holds nothing and
 * `tests/core/test_permissions.py` asserts the default role stays that way, so
 * "holds no permission" *is* "is an ordinary User", derived rather than
 * listed — and it refuses a future read-only auditor too, which it should.
 *
 * `minutes` is chosen per exchange because elevation is not one activity with
 * one shape: a stuck setting is thirty seconds and walking somebody's import
 * is ten minutes, and an Owner forced to re-elevate repeatedly asks for the
 * maximum every time. The ceiling is validated at boot to be strictly shorter
 * than the read-only session (`Settings._enforce_elevation_is_shorter_than_looking`),
 * so the ticket's "shorter-lived than" holds for every value reachable here
 * rather than for the default alone.
 */
export const viewAsElevateViewAs = <ThrowOnError extends boolean = true>(options: Options<ViewAsElevateViewAsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<ViewAsElevateViewAsResponses, ViewAsElevateViewAsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/view-as/{user_id}/elevate',
        ...options
    });
};

/**
 * Read View As Sessions
 * Recent View-as sessions across every Owner, newest first.
 *
 * Every Owner's, not the caller's own. "There is an answer to who looked at
 * what" is the ticket's checkbox, and an answer that only covers the person
 * asking is not one.
 */
export const viewAsReadViewAsSessions = <ThrowOnError extends boolean = true>(options?: Options<ViewAsReadViewAsSessionsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ViewAsReadViewAsSessionsResponses, ViewAsReadViewAsSessionsErrors, ThrowOnError, 'data'>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        responseStyle: 'data',
        url: '/api/v1/view-as/sessions',
        ...options
    });
};