# Domain
# Production: set via deploy env / GitHub secrets
DOMAIN=localhost
# Local Traefik subdomain testing:
# DOMAIN=localhost.tiangolo.com

# Used by the backend to generate links in emails to the frontend
FRONTEND_HOST=http://localhost:5173
# Staging/production example:
# FRONTEND_HOST=https://dashboard.example.com

# Environment: local, staging, production
ENVIRONMENT=local

PROJECT_NAME="TG Summarizer"
STACK_NAME=tg-summarizer

# Backend
BACKEND_CORS_ORIGINS="http://localhost,http://localhost:5173,https://localhost,https://localhost:5173,http://localhost.tiangolo.com"
SECRET_KEY=changethis
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=changethis

# Allow public signup (set false in production to disable /users/signup)
USERS_OPEN_REGISTRATION=true

# Emails
SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=info@example.com
SMTP_TLS=True
SMTP_SSL=False
SMTP_PORT=587

# Postgres
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changethis

SENTRY_DSN=

# Docker images (override in CI/CD for your registry)
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend

# TG Summarizer — AI & scraping
GEMINI_API_KEY=
API_KEY=
TOR_CONTROL_PASSWORD=
TOR_CONTROL_PORT=9051
TOR_SOCKS_PROXY=socks5h://127.0.0.1:9050
DEFAULT_PROXY_URLS=
EMBEDDING_MODEL=gemini-embedding-2-preview
DEFAULT_AI_MODEL=gemini-3-flash-preview

# Frontend (Vite build-time)
# Leave VITE_API_URL empty for local dev proxy to localhost:8000
VITE_API_URL=
VITE_API_KEY=

# Traefik dashboard (compose.traefik.yml only)
# USERNAME=admin
# PASSWORD=changethis
# HASHED_PASSWORD=$(openssl passwd -apr1 $PASSWORD)
# EMAIL=admin@example.com
