# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Self-hosted Telegram channel summarizer, migrated from a standalone app (`TG-Summarizer/`, a parity reference, may be absent from some clones) into a FastAPI + React monorepo. See `README.md` and `development.md` for the full operator/setup guide; `docs/migration/` holds the ADRs and locked decisions (`DECISIONS.md`).

## Layout

- `backend/` — FastAPI API, AI providers, scraping, APScheduler jobs, PostgreSQL (SQLModel + Alembic). Managed by **uv as a workspace whose `.venv` lives at the repo root** — run `uv sync` from the root, not from `backend/`.
- `frontend/` — React 19 + Vite, managed by **bun** (root `package.json` declares the `frontend` workspace).
- Root `.env` is authoritative for **both** halves: backend reads it via `app.core.config.Settings`; the frontend reads `VITE_*` vars from the same file (`vite.config.ts` sets `envDir` to the repo root). Do not create `frontend/.env`. `.env.example` documents every tunable.

## Common commands

Backend (native, from repo root unless noted):
```bash
uv sync                                              # install (creates root .venv)
uv run fastapi dev backend/app/main.py --port 8000   # dev server (or: cd backend && uv run fastapi dev app/main.py)
cd backend && uv run pytest tests/ -q                # tests (see isolation note below)
cd backend && uv run pytest tests/api/routes/test_items.py::test_name  # single test
cd backend && bash scripts/lint.sh                   # mypy + ty + ruff check + ruff format --check
cd backend && uv run alembic revision --autogenerate -m "msg"   # new migration after model change
cd backend && uv run alembic upgrade head            # apply migrations
```

Frontend (from repo root or `frontend/`):
```bash
bun install
bun run dev                          # Vite on :5173, proxies /api → :8000
bun run --filter tg-summarizer-frontend test:unit    # bun test src
bun run lint                         # biome check --write (no semicolons, double quotes)
cd frontend && bunx tsc -p tsconfig.build.json --noEmit   # typecheck
cd frontend && bunx playwright test  # e2e; needs backend up (docker compose up -d db prestart backend)
```

Full stack via Docker: `docker compose watch` (frontend :5173, API :8000, Swagger :8000/docs, Adminer :8080). Lint/format everything: `cd backend && uv run prek run --all-files`.

## Backend architecture

- **Three model modules, split by what the models *are*.** `app/models.py` holds the template auth models (`User`, `Item`). All TG domain models — channels, posts, summaries, bot credentials, embeddings, logs, sync metadata, and `AppSetting` (JSON settings rows) — live in `app/models_tg.py`. `app/models_rbac.py` holds `Role` and `UserRole`. Alembic's `env.py` imports all three; a model module it does not import is invisible to autogenerate, which silently produces an empty migration rather than an error. The split is **by category, not by count**: RBAC is neither template auth nor TG domain, and filing it under the nearer wrong heading would have made that module's stated purpose false. A fourth category gets a fourth module; a model that fits an existing one does not.
- **Authorisation names a permission, never a role.** *Enforced: `tests/api/test_permission_checks.py`.* `app/core/permissions.py` holds the `Permission` constants and the three seeded roles; `app/services/rbac.py` (read model) resolves a User's permissions from `rbac_user_roles`; routes gate on `Depends(require_permission(Permission.X))`. **Nothing reads `is_superuser` to decide access** — the column survives until a later ticket drops it, but two answers to "can this user do X" is drift that fails closed, so the guard forbids it. Permissions are code because they are a closed set the code names; roles are data so a fourth role is an `INSERT`. `reconcile_seeded_roles` runs on every boot because migration seeds and code constants otherwise diverge the moment someone adds a permission — the row would still hold yesterday's list, and the row is what authorisation reads. It touches **only** the three seeded ids; a role an operator added is theirs.
- **Row visibility has one seam, and it is off.** *Enforced: `tests/services/test_tenancy_seam.py`.* `app/services/tenancy.py` answers "which rows may this User see"; `TENANCY_ENFORCED` ships `False`, and while it is off `scoped_select` returns the caller's statement untouched. That is what lets the ~40 read paths that never had an owner filter adopt the seam one batch at a time without any batch changing a response — ticket 21 flips the flag once an isolation guard proves it. The flag is read in **exactly one function**, `tenancy_enforced()`, and a guard greps the whole of `app/` to keep it that way: the failure mode of a flag is always the fourteenth place it got read, which is the same shape as the two auth gates that disagreed about `/password-recovery` for months.
  - **Dispatch is by model class, because `.where(Model.user_id == user_id)` is wrong for most of the schema.** The corpus is already physically shared — `Channel.id` is the handle, `Post` is unique per `(channel_name, post_id)` — so `user_id` on those tables is a "who scraped this first" stamp, and filtering on it would hand the second follower of a channel an empty page for posts sitting right there. Those are **follow-scoped** (an `EXISTS` against `tg_channel_follows`, built in ticket 04) and their `user_id` columns are dropped in ticket 22. The EXISTS correlates on `FOLLOW_KEYS`, not on a guessed column name: `Channel` spells the join key `id` and the four corpus tables repeat it as `channel_name`, so one hard-coded name would scope four of the five against the wrong column **and still compile**. `DiscoverHandleProbe` and `SyncMeta` are **corpus**: shared and unreachable through a follow, unscoped on purpose, because an unscoped read is otherwise indistinguishable from a forgotten one. The remaining 18 tables are **user-owned**.
  - **A 404 is only half an answer; the body is the other half.** `assert_owner` refuses a foreign row as 404 rather than 403 — 403 confirms the row exists, which is the enumeration oracle signup was hardened against — but every 404 here names its resource (`"Summary not found"`, `f"{log_type} log not found"`), so a generic `"Not found"` would move that oracle into the payload instead of closing it. `detail` is a **required** keyword argument with no default, because the default is the trap. `unscoped_select(statement, reason=...)` is the matching escape hatch for reads that cross accounts on purpose (Admin export, `routes/data/admin.py`): a no-op by construction whose only job is to make the call site greppable and the reason written.
  - **Scoping a read means finding every read it makes, not the one it is named after.** *Enforced: `tests/services/test_post_tenancy_scoping.py`.* Ticket 16 moved the feed, lookup, counts and Discover onto the seam, and the Post select was the easy half. Two things were not. `list_feed` has **two query shapes**: with a per-channel cap it wraps the base select in a `row_number()` subquery, so the predicate has to go *inside* that subquery or the window ranks rows the caller cannot see and the cap is computed over the wrong set. And all three modules carried their own copy of `select(Channel.name)` to answer "do I follow this handle?" — the `unfollowed_forwarded` filter and Discover's `isFollowed` flag — which unscoped reports a candidate as followed because *somebody else* follows it. That is now `follows.visible_channel_names`, named for what it returns while the flag is off (every Channel) rather than what it returns once it flips. Review found a **fourth** copy the first cut missed, in `discover_reports.followed_names` — the one that runs *after* the aggregation and overwrites its scoped answer, so a saved report and a live one would have disagreed about `isFollowed` for identical input. Scoping the read a function is named for is the easy half; the ones that matter are the reads it makes on the way to returning.
  - **An Artifact is private, and a writable row is half the question.** *Enforced: `tests/services/test_artifact_tenancy_scoping.py`.* Summaries, chats, tag runs and Discover reports (ticket 17) are all `USER_OWNED` — an artifact is what an account *produced* over a scope, never a copy of the corpus it read, so none of the four is follow-scoped. Each family's list goes through `scoped_select` and each by-id call through `assert_owner` with **the detail string that family already answers for an absent row** (`"report not found"` stays lower-case; matching the existing string is the requirement, tidying it re-opens the oracle). The **writes are in scope too**, which the ticket's own checkboxes did not say: `upsert_*` merges into whatever row its id names, so a scoped read over a writable row lets a second account overwrite the first's summary by guessing an id — and every read guard passes throughout. An absent id still creates.
    - **The unified History gave up its own owner filter.** `/data/artifacts` hand-rolled `owner == me OR owner IS NULL` before ticket 17, written pre-emptively while nothing else scoped. Two owner filters with different NULL handling is the drift `tenancy.py` exists to prevent, and it would surface as a row visible in History and absent from `/data/summaries`. It is the one adoption that **changed a response while the flag is off** — deliberately, and confirmed before implementing, because a single-operator deployment has one account and the alternative leaves a fifth NULL rule for ticket 21 to reconcile. The predicate goes on **each leg, not the union**, because there is nowhere else to put it — the union is wrapped in a subquery projecting the labelled output columns and `user_id` is not among them, so the outside has nothing to name.
    - **`report_to_camel` answers for the viewer, never the owner.** Ticket 16 had to pass `viewer_id=report.user_id` as a commented placeholder because `get_report` had no authenticated viewer; ticket 17 gives it one and drops `followed_names`' `user_id=None` branch with it. The argument keeps its own name although the two values coincide once reads are scoped — the day a report becomes shareable they stop being the same question, and a call site spelling it `report.user_id` would answer the wrong one without changing.
  - **A dismissal is one account's judgement, and the owner is half the key.** *Enforced: `tests/services/test_discover_dismissals_are_per_account.py`.* `tg_discover_ignored` was keyed by `handle` alone (ticket 30), so the first account to dismiss a candidate dismissed it for everybody. **Scoping the read without moving the key is worse than leaving the table alone**: `ignore_channels` skips a handle that already has a row, so once A dismisses `@foo`, B's write is a no-op *and* a scoped read then tells B the handle is not dismissed — B can never dismiss it and the button silently does nothing, a functional regression rather than a visibility one. So the key, the reads and the writes moved together: composite PK `(handle, user_id)`, a real cascading FK, and existing rows adopted by `resolve_follow_owner`'s rule.
    - **This is the one family whose owner filter is *not* `scoped_select`.** The seam's filter is gated on the flag because it answers *visibility*; the owner here is part of the primary key, so filtering on it answers *identity* — which row is yours — and a flag cannot gate identity. Gated off, both accounts collide on one row again and the composite key is decoration. Every guard is therefore parametrised over both flag states, and the mutation that gates the filter fails **only** the flag-off variants: that asymmetry is the signature of the half-fix the ticket refuses.
    - **The migration settles owners rather than deferring them.** A composite primary key cannot hold NULL, so "belongs to nobody" stops being expressible the moment it runs. NULL and an id left behind by a deleted account get the same answer — the operator — because they are the same situation; a live owner is kept. A database with **no account at all** drops its rows rather than leaving them unkeyable, which is safe in the only way it can happen (that table is empty before the first superuser exists, and every read now requires a caller). It completes in one pass: alembic stamps a revision and never re-runs it, so a migration leaving work "for the next deploy" leaves it undone for ever — ticket 06's migration claims otherwise in its own docstring and that claim is wrong.
  - **A sync log is telemetry about a Channel, so it carries no owner at all.** *Enforced: `tests/services/test_sync_log_channel_telemetry.py`.* Ticket 19 moves `SyncLog` and `SyncLogPayload` to `FOLLOW_SCOPED`: the row answers "did this Channel deliver Posts, and if not why not", and the second follower of a handle has as much right to that as the first. `upsert_sync_log` still **takes** a `user_id` and deliberately does not write it, because `data_import_export._LOG_IMPORTERS` dispatches all five log types through one uniform signature; ticket 22 drops the column and the parameter together, and until then a guard hands it a real account and requires `None` on the row, since an ignored parameter decays into a written one the first time somebody tidies it. The payload table gains a denormalised `channel_name` for the same reason it already denormalises `timestamp`: the seam correlates on a real column, and reaching the parent's name through a join would put `tg_sync_logs` inside the predicate of every read of the table the payload split exists to keep cheap. **`assert_owner` on an ownerless row does not degrade, it fails closed** — `owner_id is None` raises — so ticket 18's write-takeover guard had to be restated as a Follow check rather than deleted, and it checks both the Channel the incoming log names and the one an existing row at that id already names. Deleting one row of a type nobody owns is now an administrative act (`SHARED_LOG_TYPES`, derived from `SCOPES`, never listed), which also closes the network-log hole ticket 18 left: their *reads* went Admin-only while any authenticated account could still delete one by id.
  - **An import is a door onto the same tables, and it had no lock at all.** *Enforced: `tests/services/test_import_write_scoping.py`.* Ticket 17's own argument is that a scoped read over a writable row is half a fix; it closed that for the four artifact families' endpoints and not for `POST /data/import`, which reaches Summary, BotCredential, ChatDestination and the logs through a bare `session.get(Model, id)` and an overwrite (ticket 31). The credential case rewrote another account's **token**, and because every log `upsert_*` assigns `user_id` on its existing branch too, the clobber was a takeover rather than only a rewrite. `POST /data/bot-credentials/migrate` is the same import by another name — bulk, merged by id, carrying tokens, and not even Admin-gated — so it is covered by the same rule; fixing one door and leaving the other is the mistake the ticket exists to correct.
    - **Import is per-account, because it cannot express anything else.** The ticket offered a second design — let an Admin write across accounts, since a restore that cannot restore is not a restore — and the reason for refusing it is narrower than a preference: every importer stamps a new row with the *caller's* id and an export document carries no owner at all, so a restore into an empty database already files every account's rows under whoever ran it. Refusing to overwrite a foreign *existing* row takes away nothing that worked, and needs no new permission constant. Ticket 28 is where export and import learn to carry a subject; `test_import_stamps_new_rows_with_the_caller_not_the_document` fails the moment they do, so that ticket has to re-take the decision instead of inheriting it. The route's `DATA_ADMIN` gate is not this answer — it decides who may call import, never whose rows the call lands on.
    - **A read may be gated; a write may not.** *Enforced: `test_import_write_scoping.py::test_only_reads_use_the_gated_ownership_guard`.* `assert_owner` defers to the flag, `assert_owner_on_write` does not, and which one a call site takes is the difference between a rule that holds now and one that waits for ticket 21. Ticket 31's first cut built the ungated guard for the import door and left **nine** by-id writes on the gated one — `PUT /data/summaries/{id}`, `POST /data/logs/{type}` and `PUT /data/bot-credentials/{id}` among them, all plain `CurrentUser` with no permission gate, all still rewriting another account's row on the shipping config. Review caught it. The guard now names the six functions allowed to call the gated primitive, all reads, each with a reason; anything else calling it fails. Refusing to *show* a row is a visibility change and that is exactly what the flag defers, so the reads stay gated on purpose.
    - **`assert_owner_on_write` is the seam's one non-inert function.** Ticket 30's rule, reached from the other side: overwriting a row that is already somebody else's is not a read, so refusing it changes no response on a single-account deployment, while gating it would leave the clobber open on exactly the deployment that has a second account. A flag cannot gate identity. The one flag-dependent half is **NULL**: an unstamped row stays writable while the flag is off, because legacy rows and anything a background job wrote carry no owner and refusing them would fail closed against the only account a single-operator install has. Under enforcement that flips, and an import is one transaction — so after ticket 21 the *first* ownerless row aborts a whole restore, which is why ticket 31's file asks 21 for an owner backfill rather than letting a write silently adopt the row.
    - **The two credential families had no owner check at all** — not a gated one, none — on `upsert_`/`delete_bot_credential` and the same pair for chat destinations, behind routes any signed-in account can reach. `PUT /data/bot-credentials/{id}` naming a foreign id replaced that account's stored **bot token**. Closed by ticket 31 with the rest. Deferring them was the first cut's mistake, on the reasoning that scoping a write without its read is the half-fix ticket 17 names — but that ticket's half-fix was scoping a *read* and leaving the write, which is the opposite direction. The write is identity and ungated, the read is visibility and gated; they are separable, and only one of them lets a stranger replace a token. **The matching reads** are closed by ticket 31's own review finding, as ticket 32 — see below.
  - **A read adopts the seam; it does not hand-roll the filter.** *Enforced: `tests/services/test_credential_tenancy_scoping.py`.* `list_bot_credentials` and `list_chat_destinations` were a bare `select(Model)` with no `user_id` parameter at all (ticket 32), so both list endpoints returned every account's rows. **What leaked was the id, not the token** — `bot_to_camel` returns `hasToken` and never `token_encrypted` — and the id is the part that matters, because `jobs/auto_summary.py::_auto_publish` resolves `publishBotId` and `publishChatId` by id with **no owner check at all**, while `routes/telegram.py::_resolve_bot_token` makes exactly that check on the interactive path. Scoping the list takes another account's credential ids out of the UI; it does not take them out of reach. That door is ticket 33, below.
    - **The scheduler sent as whichever bot the Summary named.** *Enforced: `tests/services/test_auto_publish_scoping.py`.* Ticket 33. `upsert_summary` passes unknown body keys straight into `Summary.extra`, and credential ids are **client-chosen strings** taken from the path of `PUT /data/bot-credentials/{bot_id}` — guessable, not random. So a Summary could name another account's `publishBotId` and `publishChatId`, and the scheduler would decrypt that account's token and send as its bot to a destination that account chose. The check goes in **`publish_summary_text`**, not only in `_auto_publish`: that is the function that decrypts, and guarding the caller leaves the next caller unguarded — the `/password-recovery` shape, and ticket 31's nine by-id writes. It refuses *before* `decrypt_token`, because a refusal that arrives after the plaintext exists has already produced the thing the encryption is for. `acting_user_id` is a **required keyword with no default**, ticket 32's lesson: an optional actor leaves every existing call site passing nothing and still passing its tests. The exploit is **latent rather than live** on the shipping config, because `run_auto_summary` still selects `Summary.user_id == operator OR IS NULL` — that filter goes away with the operator model, and this is a token decryption reached by a guessable id, so it closes now.
      - **A refusal writes a failed publish log; it does not return quietly.** The scheduler is unattended, so a silent `return` makes "auto-publish is misconfigured" and "auto-publish is off" the same observation. An **absent** destination used to do exactly that, so absent and foreign now answer alike, with the same text — `assert_owner`'s rule that the body is the other half of the answer, applied where there is no status line at all to carry it.
      - **`may_act_on` is `assert_owner_on_write` without the raise**, and both callers here are in the scheduler with no response to put a 404 in. Stating the rule once is the point: two spellings of "is this row mine" is the drift the seam exists to prevent, and they diverge on the NULL branch first. A NULL on **either** side — the row's owner or the actor's — is unanswerable, so it is permitted while the flag is off and refused under enforcement. The actor half is not hypothetical: `run_auto_summary` deliberately picks up ownerless Summaries. This is the opposite of what ticket 32 did to the credential *lists*, and deliberately so — handing every account the deployment's stored credential is a leak, declining to use one is not. It is another line on **ticket 21's owner-backfill bill**.
      - **The interactive twin was the divergent one, and review caught it.** The ticket cites `routes/telegram.py::_resolve_bot_token` as the reference implementation — but it hand-rolled the rule, and consolidating the scheduler's copy while leaving the twin spelling it differently is the half-fix this repo names. It answered **403 `"Bot credential not accessible"`** for a foreign row against 404 `"Bot credential not found"` for an absent one, over client-chosen ids: an enumeration oracle any signed-in account could walk, on a live route. And it refused only a *stamped* foreign row, so under enforcement that door would publish with an unstamped credential the scheduler refuses. Both now take `may_act_on` and answer 404 with the family's own string. The 403 is not a lost response: on a single-account deployment there is no foreign row to refuse, and `isAuthFailure` never keyed on that detail.
      - **`may_act_on` has a declared caller list, walked from the AST.** It is the most inviting of the seam's three primitives — returns a bool, needs no `detail` — and it does **not** consult the flag on the non-NULL branch. A by-id *read* adopting it would narrow a response while enforcement is off, which is the one thing no seam adoption may do. Its two arguments are **keyword-only** for a smaller reason with the same shape: they are the same type and the function is symmetric in them, so a transposed positional call would compile, pass every behavioural test, and answer whether the actor is owned by the row.
      - **The wiring guard was watched passing its own mutation.** Attributing the send to `dest.user_id` instead of `summary.user_id` passed all nineteen tests, because every one gave the Summary and the destination the same owner — the wrong id and the right id were the same id. Only an **ownerless destination** separates them, and that row shape now carries the guard.
    - **"The last unscoped read family" was ticket 32's claim, and it was wrong.** *Enforced: `tests/services/test_setting_group_and_job_scoping.py`.* Three families were unaudited, and ticket 35 is where they moved onto the seam: `list_setting_groups` hand-rolled `user_id == me OR user_id IS NULL` over `ChannelSettingGroup`, `load_groups_by_id` read the same table with no filter at all, and `_running_job_from_row` read `SyncJob` across accounts to serve `GET /jobs/runtime-config`. All three now take a `user_id` with no default — an optional one leaves every existing caller passing nothing and still passing tests. The guard that earns its place is the **flag-off** one: a hand-rolled `.where(Model.user_id == user_id)` reaches the same answer under enforcement and fails there, because it narrows in a state where the seam promises not to. The backfill was **ticket 34's**, above — it was ticket 21's until three tickets had each deferred it and it earned a ticket of its own.
      - **`list_setting_groups` was already narrowing on the shipping config, so its flag-off answer moves.** This is the second adoption in the programme to change a response with the flag off, and the first to do it because the *old* code was the violation: `me OR NULL` filtered in both states. Flag-off is now unfiltered, on ticket 17's precedent and for its reason — a single-operator deployment has one account, and keeping the old filter leaves a fifth NULL rule for ticket 21 to reconcile against four that already disagree with it. Under enforcement it is `user_id == me`, so the built-in presets a fresh install seeds with no owner disappear; that is pinned rather than papered over, and is one more line on ticket 21's owner bill.
      - **The orphan rescue is scoped through the follow, not through `Channel.user_id`.** A channel can name a group the caller does not own, because auto-follow files the Channel under whoever scraped the handle first, and dropping that group leaves the channel showing a policy the settings screen cannot explain. The rescue used `operator.distinct_operator_setting_group_ids`, which hand-rolled `Channel.user_id == me OR IS NULL` — a question whose column ticket 22 deletes. It is now `scoped_select(..., Channel, ...)`, an EXISTS against `tg_channel_follows`, and the old helper is gone rather than left beside it.
      - **`load_groups_by_id` is excused, and the excuse is functional rather than a preference.** Every one of its seven call sites does `groups_by_id.get(channel.setting_group_id)` for a channel already scoped, so a second filter hides nothing from anybody — it blanks the *policy* of a channel the caller legitimately follows, and three call sites read a missing group as "skip this channel": `auto_sync` continues past it, `bulk_channels` refuses the reset, `get_group_for_channel` raises a 500. So it goes through `unscoped_select(reason=...)`, which is a no-op whose whole value is that the reason is written and greppable. It deliberately takes **no** `user_id`: ticket 19 had to guard an ignored parameter on `upsert_sync_log` because a uniform importer signature required one, and nothing requires one here.
      - **The `SyncJob` fix is not the row read the ticket named.** `get_active_sync_job_summary` *prefers* `_active_jobs` and only falls back to `_running_job_from_row`, so scoping the named function alone leaves the preferred path answering across accounts — on the worker, which is the one process where that dict is populated. `_job_is_visible_to` restates `scoped_select`'s `USER_OWNED` rule against a dict, because the seam cannot reach process memory. It must not borrow `may_act_on`: that does not consult the flag on its non-NULL branch, so it would narrow a response while enforcement is off. The first cut did exactly that and `test_auto_publish_scoping.py`'s declared-caller list caught it — the guard's own error message names this case.
      - **Auditing the reads found three by-id writes with no owner check at all**, which is the larger hole. `update_setting_group`, `delete_setting_group` and `bulk_assign_setting_group` each resolved a group by client-visible id behind a plain `CurrentUser` route: rename another account's group and reschedule every channel in it, delete it, or govern your own channels by their policy row. All three take `assert_owner_on_write` — ungated, so nothing moves on a one-account deployment while the clobber closes on the one that has two. The mutation that swaps in the *gated* `assert_owner` fails **only** the flag-off variants, which is the half-fix signature ticket 31 names.
      - **The filter that remains answers identity, and must not adopt the seam.** What is left of the old helper decides whether a *name* is taken, mirroring the unique index `(COALESCE(user_id::text, 'global'), lower(name))`. Ticket 30's rule: a flag cannot gate identity. Putting `scoped_select` there would make a duplicate name stop being rejected while enforcement is off and start arriving as a Postgres `UniqueViolation` instead of the route's 409. It stays *wider* than the index — `me OR NULL` — because narrowing it to match would start allowing names the operator has been refused since the presets were seeded; ticket 22 can reconcile the two once the global rows are gone.
      - **`bulk_assign_setting_group` was not the only writer, and the scraper is not a client.** `sync_orchestrator`'s chat-id freeze went through it, which under the new check would have made a scraper-internal freeze depend on a user-facing guard. The per-channel move is now `apply_group_to_channel`, called directly there. That is not a hole in the check: the check exists because `bulk_assign` takes a **client-chosen** group id, and the freeze resolves its group from the Channel's own owner one line above, so there is no id for a stranger to name. `move_channel_to_restricted_group` is the same shape and predates it.
    - **`IMPORT_WRITES` places or excuses every table the import writes, and its guard walks the AST.** The first version compared the inventory against two hard-coded sets, so a new `_import_*` section writing a new table would have left the suite green while the docstring claimed otherwise — the same false-confidence shape as a guard that cannot fail. Sync logs are the one excused family with rows to excuse: they store no owner (ticket 19), and the API door's follow check is *create-only*, which a restore cannot adopt without refusing every re-import. Network logs are **checked**, matching `create_logs` — the first cut excused them off `PERSONAL_LOG_TYPES`, which is a *retention* partition, and borrowing it to answer a write-authority question left the two doors disagreeing.
  - **Every user-owned row has an owner before the flag flips, and the inventory is derived rather than listed.** *Enforced: `tests/services/test_owner_backfill.py`.* Ticket 34. Three tickets reached this requirement independently and each deferred it: under enforcement an unowned `USER_OWNED` row is invisible to every account, refused to every reader by id, and unwritable — and an import is one transaction, so the *first* one aborts a whole restore (ticket 31). `backfill_user_id.py` existed and was not the answer: **nothing runs it**, `prestart.sh` runs only `alembic upgrade head`, and its thirteen models predate `SCOPES` — five are now follow-scoped or corpus whose `user_id` ticket 22 drops, and it misses ten user-owned tables added since. So it is a migration, over the 14 tables `owner_backfill_inventory()` derives from `SCOPES`; the four composite-key tables excuse themselves because `user_id` sits in a `NOT NULL` primary key, which is a stronger excuse than a sentence.
    - **The migration freezes its list and the guard derives one.** The obvious version imports the derivation and calls it, and it is wrong in both directions: an applied revision must keep meaning what it meant, so reading live app code makes it drift and breaks `upgrade head` from empty the first time somebody renames the function — and a table added *after* the revision ran is not reached by re-deriving anyway, it needs its own. The derivation therefore lives in the guard, where a forgotten table is a red test rather than a row that vanishes on the flip.
    - **A payload row inherits its parent's owner; it is not adopted.** `SummaryPayload` and `ChatSessionPayload` are reachable only through the row that names them, so stamping them with the operator while the parent belongs to another account leaves a detail view whose body is gone and whose parent is still listed — `SyncLogPayload`'s rule from the other side. The inheritance pass runs **before** the operator pass, and the wrong order is invisible on a single-account database, because there the parent's owner *is* the operator. Only a second account separates them, so the guard seeds one; that mutation fails exactly one test and no other.
    - **One table cannot be stamped at all, and the plain `UPDATE` stops the deploy.** `tg_channel_setting_groups` carries a unique index on `(COALESCE(user_id::text, 'global'), lower(name))` — the only non-key unique index on any of the fourteen, checked by the guard rather than assumed. Every database ever migrated from empty holds global-scope presets and the operator holds identically-named copies, so setting the global rows' owner makes both halves of that key equal; the index refuses it, and because the statements share one transaction the revision fails and `prestart.sh` stops the deploy. Those rows are reconciled one at a time instead — merged into the operator's same-named group with `tg_channels` **and `tg_channel_follows`** repointed first (`m5n6o7p8q9r0` did the same merge before follows existed, so copying it verbatim strands every follow), or adopted where the operator has no counterpart. Row by row rather than as a set, because two deleted accounts that each had a `default` collide with *each other* one step further along. **The guard could not have caught this as written**: its seeder invents a unique name per row, so the index was structurally unreachable — a guard that covered the `UPDATE`'s predicate and nothing about what the `UPDATE` had to satisfy.
    - **The two no-account databases are not the same database.** `_bootstrap_owner` returns nothing both when a deployment has accounts but no resolvable superuser and when it has no accounts at all, and the right answers are opposite. The first is refused, loudly, naming the tables and counts — somebody has used it and the rows may be their content, and alembic offers no second chance. The second is a **fresh install**: `prestart.sh` migrates before `init_db` creates the first superuser, and the setting-group migrations seed three global presets when they find no user, so refusing there breaks every first deploy the project will ever have. The first cut did exactly that, and the guard caught it on its first run — `tests/conftest.py` migrates an empty database the same way a deployment does, so that path cannot go green while broken.
    - **The columns stay nullable, and that is not an oversight.** Every log `upsert_*` still takes `user_id` as optional and the scheduler still creates `SyncJob` rows without one, so `NOT NULL` would trade a data gap for an outage. A fresh install also keeps its global presets, for the same reason it cannot adopt anything. Both reduce to one requirement, and it is **ticket 21's**: eliminate the `user_id=None` creation paths before flipping the flag, rather than merely flipping it. An earlier draft of the migration claimed nothing could reference a global preset; review showed `channels.py` and `followed_channels.py` both resolve one through an optional `user_id`. Dropping the superseded columns stays ticket 22's.
  - **Every table is placed or excused, with a reason.** `SCOPES` classifies 27 tables; `OUT_OF_SCOPE` says why `AppSetting`, `User`, `Item`, `Role`, and `UserRole` are not the seam's business — `tg_app_settings` because after ticket 06 it is deployment-wide with `key` as its whole primary key, so there is no per-user row to hide. A table nobody classified fails the guard, which is the only moment when "whose rows are these?" is cheap to answer — and the guard walks subclasses **recursively**, because `User` and `Item` descend from `UserBase`/`ItemBase` and one level of `__subclasses__()` misses exactly those two.
- **A Follow is the relation; the Channel and its Posts are not.** *Enforced: `tests/services/test_channel_creation_paths.py`, `tests/services/test_follows.py`.* `tg_channel_follows` (ticket 04) carries what is private about watching a channel — setting group, followed-at, tags, start id, start time, discovery provenance — because those columns sat on `tg_channels`, where the second follower of a handle would have to overwrite the first one's values to have any of their own. `app/services/follows.py` is the aggregate and the sole writer. `next_sync_at` is on the row although nothing reads it yet: it is the column the deferred most-eager-wins scheduling needs, and adding it later means a migration on a table with a row per user per channel.
  - **Every path that creates a Channel writes a Follow, and a guard walks the AST to say so.** Three modules create Channels (`channels.py`, `followed_channels.py`, `data_import_export.py`); a fourth that forgot the follow would insert happily and read fine right up until ticket 21 flips enforcement, at which point the Channel is invisible to its own creator. The guard requires each creator to be **declared with a reason and to call a follow writer** — declaration alone is bookkeeping, and a declared module that quietly stopped writing follows would pass.
  - **An ownerless Channel's follow goes to the operator, in one function.** `Channel.user_id` is nullable and `ChannelFollow.user_id` is a real FK. The two honest answers to `user_id=None` are "write no follow" and "write one owned by the first superuser", and the first manufactures exactly the drift `scripts/audit_tenancy_drift.py` exists to report. `resolve_follow_owner` is that rule, so the backfill and the dual-write cannot disagree about who owns a legacy row.
  - **Removing a Channel is unfollowing it; the delete is deferred, not gone.** *Enforced: `tests/services/test_unfollow.py`, `tests/jobs/test_retention_collects_unfollowed.py`.* `DELETE /data/channels/{id}` used to drop the Channel row and bulk-delete every Post under it, for everybody, with no ownership check — so the second follower of a handle lost a scrape they had nothing to do with because the first follower tidied their list. It now drops one `(user_id, channel_id)` follow and touches nothing else, answering **404, not 403** for a channel the caller does not follow, with a detail that names the resource. `collect_unfollowed_channel` is the other half: retention reclaims a Channel once `channel_ids_without_follows` says nobody holds it, taking the Posts, embeddings, translations and sync state with it, because none of those has a foreign key to `tg_channels` and nothing would otherwise cascade. There is deliberately **no grace window** — re-following before the next retention pass already keeps everything, since the check is made at collection time rather than recorded at unfollow time. The route function keeps the name `delete_channel` because the generated client derives `dataDeleteChannel` from it; the *service* is named for what it does.
  - **Nothing deletes on the strength of a missing follow until the backfill says the table is authoritative.** *Enforced: `tests/jobs/test_retention_collects_unfollowed.py`.* An absent row in `tg_channel_follows` reads identically whether nobody follows the channel or nobody has written the row yet, and the two have opposite consequences. Retention fires ~60s after every boot, so the ungated collection deletes **every channel and every post** on any database whose backfill has not run — the native dev flow never invokes `prestart.sh`, and a restored pre-ticket-04 backup carries no marker. `follows_backfilled()` is that check and `FOLLOWS_BACKFILL_KEY` lives in `services/follows.py` because `app/` cannot import from `scripts/`. The operator's retention windows are no defence: collection ignores them, so `postRetentionDays: 0` would not have saved that database.
  - **The channel list does not filter on follows yet, and a removed channel stays visible until retention collects it.** That gap is ticket 05's, closed by ticket 15. Scoping the list here would mean changing a response while enforcement is off, which is the one thing the seam's batches are not allowed to do. Do not "fix" it by adding a follow filter to `list_channels` ahead of that ticket.
  - **`ON CONFLICT DO NOTHING` reports through `RETURNING`, never `rowcount`.** SQLModel's `session.exec` is built for reads and wraps the result, so `rowcount` on it stops meaning rows affected — it claimed a row inserted for a conflict that inserted nothing, which is the one question the return value answers. Through `session.execute` it is unreliable for DO NOTHING too. A row comes back only when one was written.
- **Settings are two tables, and a key belongs to exactly one.** *Enforced: `tests/services/test_settings_table_split.py`.* `tg_app_settings` is deployment policy (PK `key`, one row for everybody); `tg_user_settings` is personal (PK `(key, user_id)`, a real cascading FK). `services/settings_registry.py` classifies every key with a sentence saying why, and the two aggregates — `settings_store.py` for global, `user_settings.py` for per-User — each refuse the other's keys. Two tables rather than one with a nullable owner, because a nullable owner is the `operator.py` ambiguity: `user_id IS NULL` reads identically as "belongs to the deployment" and "nobody stamped it", and those diverge the moment ticket 21 flips enforcement. The old table's `user_id` survives as a "last written by" stamp and scopes nothing; ticket 22 drops it.
  - **The `sync` blob was three things, so it is now three rows.** Deployment scheduler policy (`syncConcurrency`, the tick interval, the failure backoff) stays global under `sync`; the scheduler's own counters (`consecutiveFailures`, `autoSyncPauseUntil`, the partial-sweep cursor) move to global `sync_runtime`; the per-channel defaults a person picks (`globalStartTimeMode`/`Value`, the dynamic-sync defaults) become per-User `sync_prefs`. **Every writer used to read-modify-write the whole blob**, so saving a start-time preference wrote back whatever counter that browser last read, and the scheduler bumping a counter wrote back stale preferences. Splitting the rows is what removes that — a permission check would not have, because the last writer would still win.
  - **The endpoint is a facade; the carve is invisible on the wire.** `GET`/`PUT /data/settings/sync` keep their exact old shape — `load_sync_settings` reassembles the three rows and `save_sync_settings` routes each field to its home — so neither the browser nor the generated client changed. Runtime fields are **routed, not dropped**: three frontend call sites (`App.tsx`, `commands/actions.ts`, `commands/extended-commands.ts`) pause and resume auto-sync by PUTting `autoSyncPauseUntil` to this endpoint. Dropping would have been the tidier rule and a silently broken Pause button. What actually protects the counters is that `buildSectionPayload("sync", …)` is built from the frontend settings schema, which declares no runtime field — so a settings save physically cannot name one.
  - **A per-User key with no owner is an error, not a fallback.** `save_settings_section` raises rather than resolving a missing owner through `get_operator_user_id`, which is the NULL fallback the plan's decision 24 dissolves. The reads that need the preference half already have a user in hand: `sync_orchestrator` has `effective_user_id`, `bulk_follow._load_effective_start_time` had the parameter and was ignoring it, and `build_runtime_config` already took one. The scheduler passes none and needs none — it reads only policy and runtime, both global.
  - **The migration is idempotent because it runs on every deploy.** `prestart.sh` calls `alembic upgrade head`, so the carve is written to be a no-op on a second pass: once the `sync` row holds only policy fields there is nothing left to extract, and both inserts are `ON CONFLICT DO NOTHING`. On a database with **no account yet**, the preference fields are left in the `sync` row rather than dropped, so the next deploy finishes the move — losing real settings to save one query is the wrong trade.
- **Retention deletes on four windows, chosen by what the rows are.** *Enforced: `tests/jobs/test_retention_split_four_ways.py`.* Ticket 20 carved `retention` the way ticket 06 carved `sync`, for a sharper reason: one blob meant one `postRetentionDays`, and until ticket 18 gated the write any account could set it to 1 and destroy every account's Posts on the next sweep. Gating closed that by permission; the carve closes it by construction. The **corpus** (Posts and the embeddings, translations and sync state keyed to them) runs on the deployment's `postRetentionDays` with **no owner filter at all** — `Post.user_id` is a "who scraped this first" stamp ticket 22 drops, and filtering on it deleted the first follower's rows while leaving the second follower's identical ones behind. **Personal logs** (publish, LLM, embedding) run on their owner's `logRetentionDays`, and **Discover reports** on their owner's two caps.
  - **`sharedLogRetentionDays` is the new field, and it exists because two families and one row-state have no owner.** Sync logs are Channel telemetry (ticket 19), network logs are proxy behaviour (decision 23), and `user_id` is nullable on all five log tables — every `upsert_*` takes it as an optional argument, so a row written by a background job carries no owner as a matter of course. Once the personal families moved to per-account windows, all three were reachable by **no** window: a leak that looks exactly like retention working. `SHARED_LOG_TYPES` and `PERSONAL_LOG_TYPES` partition the five families and are both derived from `tenancy.SCOPES`, never listed, so a family cannot fall out of both or into both.
  - **A network log carries a `user_id` and it still decides nothing.** It is the id of whoever triggered the request, not an owner, so the family runs on the deployment window — the one place a present stamp is deliberately ignored. The guard for it disables the deployment window first and asserts the row survives the account's own; disabling the *personal* window instead makes the test pass no matter which sweep takes the row, which is the version that was written first and caught nothing.
  - **The Admin log purge stopped pretending to scope.** `delete_old_logs` narrowed itself to `user_id == operator OR IS NULL` while its docstring said it swept every account — an administrative sweep quietly skipping everybody else's rows. Ticket 19 removed the last argument for that filter and ticket 20 removed the filter. `expire_sync_payloads_stmt` lost its owner argument for the same reason; narrowing it was how payloads outlived the log rows they belonged to.
  - **A retention field with no owner raises, where the `sync` facade drops.** `save_retention_settings` refuses a personal field with no `user_id` instead of skipping it, because unlike `sync` nothing here writes without an account — the scheduler writes `sync` and would fail every tick. Dropping is how a window silently keeps its default while the caller believes it saved; the raise found four call sites that were doing exactly that.
  - **Legacy ownerless Discover reports are adopted once, in the migration.** Per-account pruning can never reach a report with no `user_id`, and every report written since ticket 17 has one — so this is a one-time adoption, not a rule the job keeps applying. It resolves the owner through the same `FIRST_SUPERUSER`-then-oldest-superuser rule as `resolve_follow_owner` and the ticket 06 migration, so the three cannot disagree about who the operator is. Logs are **not** adopted: new ownerless log rows appear every day, so adoption would fix nothing and `sharedLogRetentionDays` is the standing answer instead.
- **The quota ledger counts Requests, and one Request is one `fetch_with_retry` call.** *Enforced: `tests/services/test_quota_ledger.py`, `tests/api/test_quota_usage_route.py`.* `tg_quota_usage` (ticket 08) records what each account spent, PK `(user_id, day, budget)`, and **nothing reads it to decide anything** — ticket 23 picks a lane from it and ticket 24 refuses at the ceiling. `services/quota.py` is the aggregate and sole writer. The unit is an HTTP Request to the Telegram web view rather than a channel sync, because one sync is anywhere between one request and fifty and a limit denominated in syncs is not a load control (decision 15). `day` is a DATE because the reset is UTC midnight; a timestamp would push that boundary out into every reader, where they would eventually disagree about it.
  - **Charged per attempt Telegram answered, not per call.** A 404, a 429 and a soft-blocked web view all cost Telegram what a 200 costs it, so all three count (decision 20); a connect timeout or a dead proxy counts nothing. The charge happens inside the retry loop, not at the exit: `httpx.HTTPStatusError` subclasses `httpx.HTTPError`, so a 404 satisfies `is_network` and goes round again up to `NETWORK_FETCH_RETRIES` (**8**), and one charge per call billed eight real round trips as one — worst for the accounts under the most rate-limit pressure. Review caught that; every guard had passed because every guard used `retries=1`. `TelegramWebViewUnavailable` is a `ConnectionError` subclass, so the obvious `is_network` test files a soft block with the dead proxies; it reads the soft-block flag instead. Retries inside one call are **not** each a Request: "a flaky proxy is not the User's doing", and charging attempts would make the Budget measure proxy health rather than demand.
  - **The count travels by `contextvars`, not by a parameter.** `core/request_meter.py` — in `core/` rather than `services/` for the reason `async_db.py` is an exception there: infrastructure with no domain in it. `run_sync_job` and `bulk_follow.run_follow_job` each open a meter, every fetch underneath increments whatever is active, and completion reads the total. Meters **nest** — the inner block restores the outer rather than clearing the slot — so the sync a follow job chains is charged separately and not twice. Bulk follow is metered because its probe phase is one fetch per handle and a batch runs to hundreds; `jobs/discover_probe.py` is deliberately **not**, because the probe queue is corpus-scoped and no account owns an entry to charge. Threading a user id from the job through the orchestrator and the scraper into the HTTP client would put a parameter nobody on that path reads into a dozen signatures. A module-level counter would charge two concurrent jobs for each other's work and would do it **only** under concurrency, which is the failure that never reproduces in a single-user test. **No meter active means no counting**, which is what every non-sync caller of `fetch_with_retry` gets — the Bot API, thumbnails, proxy health checks. Only `is_telegram_web_url` hosts count at all.
  - **Three Budgets, read off the one field that already tells them apart.** `SyncJobState.sync_mode`: `auto` → `auto_sync`, `individual` → `manual_single`, `bulk`/`sync_all`/`recheck_restricted` → `manual_bulk`. A total function over that Literal, not a new field somebody has to remember to set, so a sixth mode fails in `quota.py` rather than being filed under whichever Budget an `else` branch named.
  - **Charged once at completion, from a `finally`.** Decision 19's "account at completion": the actual count, not the guess an enqueue-time charge would use. In a `finally` so a job that dies part-way still pays for the pages it fetched — the alternative is a discount for failing. A charge of zero writes no row, because a row of zero is indistinguishable from a real day of zero usage. `charge_sync_job` swallows and logs its own failures, which is right while nothing reads the ledger and **wrong once ticket 23 enforces**: a charge that silently fails is then free work.
  - **Never pruned, and the guard says so in both directions.** "Kept forever" is what makes the ledger worth reading — an Admin setting next quarter's limits is looking at last quarter's numbers. Retention and `stats.clear_table` both work from explicit inventories, and the guard asserts the ledger is on neither rather than trusting nobody adds it. The cascading FK still applies: an account's ledger going with the account is that account ceasing to exist, not a prune.
- **Thin routes, fat services.** `app/api/routes/*.py` are thin; business logic lives in `app/services/*.py` (e.g. `routes/data/channels.py` → the `channels.py`/`posts.py`/`credentials.py` services). **`/data` is a package, one module per resource family** (`routes/data/`): it was a single 1,453-line router until C1 split it. The parent router in `data/__init__.py` owns the `/data` prefix and the `data` tag, so operation ids stay `data-<function_name>` — never change a route function's name without expecting the generated client to change with it. `tests/api/test_route_inventory.py` asserts every declared route is actually mounted. `sync_orchestrator.py` drives channel sync (backward pagination, auto-follow); `proxy_pool.py` gives per-proxy lane semaphores; `scraper.py` fetches/parses `t.me`. AI providers are pluggable under `app/ai/providers/` behind a registry (`app/ai/registry.py`).
- **Every route declares a response model.** *Enforced: `tests/api/test_route_module_hygiene.py`.* Request *and* response models live in `app/schemas/<resource>.py` — never inline `BaseModel` in a route module. A route returning `dict[str, Any]` becomes `{"additionalProperties": true}` in OpenAPI and `Record<string, unknown>` in the generated TypeScript, which is why the frontend hand-maintains duplicate domain interfaces that no compiler keeps in step. `app/schemas/summaries.py` is the reference (B1); shapes shared across families (e.g. `StatusResponse` for `{"status": "deleted"}`) go in `app/schemas/common.py`.
  - **A list view must not carry a field only its detail view renders.** Twice now: `/data/summaries` shipped 26 MB a page, and `/data/logs/sync` shipped **56.28 MB for 500 rows, 99.7% of it request/response bodies**, for a viewer that expands one row at a time. The shape both times is *list light + `GET .../{id}` full*, with the text search moved into SQL so the dropped fields stay findable without being sent (`services/summaries.py::_search_clause`, `services/logs.py::LOG_SEARCH_COLUMNS`). Splitting a table is not enough on its own — `tg_sync_log_payloads` had existed for weeks and the list joined it straight back in.
  - **A corpus-sized field does not belong in an open `extra` column.** TOAST is all-or-nothing per value, so reading *one small flag* out of `extra` detoasts everything in it. `Summary.extra` held `citedPosts`/`promptText`/`chatMessages` and a 49-row page cost 26 MB and 2.69 s to return 1.15 MB. They now live in `tg_summary_payloads`; `tg_sync_log_payloads` is the same shape for the same reason. **Pushing the projection into SQL does not fix this** — it was measured at 2.86 s, because the detoast and parse happen server-side either way. Split it into a companion *table*, not a sibling column: a table is fail-closed, a deferred column is one forgotten `defer()` away from the original cost.
  - **Rows with an open `extra` JSON column** (`Summary`, and anything else merging `extra` into its payload) use `model_config = ConfigDict(extra="allow")` and declare only the *always-present* columns. Do **not** declare a conditional key just to be thorough: a declared optional field serialises as an explicit `null` where the key is absent today, which silently changes the wire format. Let conditional keys flow through `extra`, and say so in the model docstring.
  - Migration progress is measurable — count `$ref`-typed 200 responses in `frontend/openapi.json` (see §6 of `docs/architecture-simplification-plan.md`). It was 26/129 before B1.
- **Every service module is one of five kinds.** *Enforced: `tests/services/test_service_kinds.py`, which holds the full per-module inventory — that file, not this list, is the authority.* `app/services/` has 52 modules; without a rule, every new feature re-litigates where its code goes. New code must fit one kind, and say which:
  1. **Aggregate** — owns one table and is the *only* module that writes it (`channels.py`, `posts.py`, `summaries.py`, `discover_reports.py`, `settings_store.py`). A **companion payload table** belongs to its parent's aggregate, not to a module of its own: `logs.py` owns `tg_sync_log_payloads` and `summaries.py` owns `tg_summary_payloads`, because the split is a storage detail the API never sees and only one module should know the row is really two.
  2. **Read model** — read-only aggregation across tables; takes a `Session`, never commits (`discover.py`, `runtime_config.py`). *Not `stats.py`* — this list used to claim it was one, but `clear_table` deletes across every aggregate's table and commits, so it is a declared exception.
  3. **Integration** — owns one external boundary (`scraper.py` → `t.me`, `network.py` → HTTP/proxy, `publish.py` → Bot API, `embeddings.py` → provider).
  4. **Pure transform** — no `Session`, no network, trivially testable (`post_media_parser.py`, `post_reply_parser.py`, `channel_tags.py`, `serialization.py`).
  5. **Orchestrator** — owns one workflow and coordinates the other four (`sync_orchestrator.py`, `bulk_follow.py`, `data_import_export.py`).

  **Never split a module because it got long.** A file extracted only to reduce a line count must fit one of the five kinds or be merged back — decomposing `sync_orchestrator.py` means extracting named *stages*, not slicing lines. Deliberate exceptions live in `EXCEPTIONS` in the test, each with a reason: `async_db.py` is an infrastructure utility that belongs in `core/`, not `services/`; `followed_channels.py` writes `Channel` alongside `channels.py` so the Discover and auto-follow paths share one creation path; `stats.py` is a read model carrying one destructive admin operation.
- **Router assembly.** `app/api/main.py` builds the `/api/v1` router; `private` routes mount only when `ENVIRONMENT == "local"`. `app/main.py` adds `APIKeyMiddleware`, CORS (outermost, so preflight beats auth), the lifespan (startup checks → `init_db` → `start_scheduler`), and a middleware that returns **410** for any `/api/*` not under `/api/v1/*` in production.
- **Auth dependencies** (`app/api/deps.py`): `SessionDep`, `CurrentUser` (JWT), `get_current_active_superuser`. **Mode A** (single-operator, hardened) is the deployment model: all AI/RAG/network/telegram/jobs routes require auth; in staging/production a request must carry a JWT **or** `X-API-Key` (fail-closed); raw bot tokens in request bodies are rejected outside `local` (use stored `credentialId`). One superuser owns all data — no per-user row scoping yet.
  - **Registration and approval are two separate switches** (ADR-011, which supersedes Mode A's "open registration off in production"). `USERS_OPEN_REGISTRATION` decides whether strangers may apply; `USERS_REQUIRE_APPROVAL` (default **off**) decides whether an application waits for an Admin. Approval is **not** enforced at login — an unapproved person gets a token and can read `/users/me`, and every data-bearing router refuses them with `PENDING_APPROVAL_DETAIL`, which is what lets the app show `/pending-approval` instead of a permission error on every action. That gate is mounted **per router in `api/main.py`**, not per route: *enforced by `tests/api/test_approval_gate.py`*, which inverts the check so an unrecognised router is treated as a hole rather than exempt. `POST /users/signup` answers 202 with one fixed **message** for every address — returning `UserPublic` would reopen the account-enumeration oracle by construction, because a body with an id in it only exists when creation happened.
  - **Two gates decide reachability, and they must agree.** `APIKeyMiddleware` runs *before* routing and 401s anything not in its public list; the route's own dependencies decide the rest. Nothing kept them in step, and they drifted: `/password-recovery/{email}` and `/reset-password/` sit on a prefix-less router mounted at `/api/v1`, so the middleware's `/api/v1/login` prefix never matched and forgot-password answered 401 outside `local`. *Enforced (both directions): `tests/api/test_public_route_exemptions.py`.* Add a public route and you must add its exemption; protect one and you must drop it. **Both gates answer 401**, so a test asserting a status code cannot tell which one spoke — assert the `detail` string (`"Authentication required"` is the middleware, `"Not authenticated"` is the route). And the middleware's exemptions must be **settings-independent**: policy like "registration is closed" belongs in the handler, which already answers 403, not in a second gate that has to be kept in step with the first.
- **Two processes: the API serves, the worker syncs.** *Enforced: `tests/deployment/test_worker_count.py`.* Ticket 10 moved APScheduler and every lane consumer into `app/worker.py`, run by its own single-replica `worker` compose service (`python -m app.worker`). The API process starts **neither** the scheduler nor a lane consumer, and a guard reads `main.py`'s AST to say so — the point of the split is that deploying the API no longer aborts an in-flight scrape, and any local drain hands the scraping straight back to the tier it was taken from. `reconcile_interrupted_jobs` moved with it and is now the **worker's alone**: it marks every non-terminal job failed on the reasoning that in-memory progress died with the process, which stopped being true of the API the moment the sync left it. Native dev needs the worker started separately (`development.md`) — without it the app runs and silently syncs nothing.
  - **The image still runs `--workers 1`, for two of the original three reasons.** It shipped as `--workers 4` (template default, never reconciled with the scheduler) and each worker ran its own APScheduler — four `Auto Sync (scheduler)` jobs per tick, 4x every scheduled job, 711 rows stranded in `running`. The scheduler reason is gone; the job registry (`scraper_jobs`) and the per-proxy semaphores (`proxy_pool` — N processes means N x the request rate at Telegram) remain, and the latter is why the **sync tier is pinned to one replica**. The guard asserts the reasons that survive, so **it fails when one is externalised** — that failure is the signal a step is done, not an obstacle. Sequenced plan: `docs/scaling-to-multiple-workers.md` (steps 1 and 4 done, 3 is ticket 13). **Scale the API tier, never the sync tier.**
  - **Every sync mode enqueues, one message per Channel** (decision 30). `app/jobs/sync_queue.py` is the consumer for all six lanes (`sync_lanes.DRAIN_ORDER`); `POST /jobs/sync`, `run_auto_sync` and bulk follow all call `enqueue_sync_job`. A message is one Channel so it can be attributed, timed out and failed on its own; the batch lives on in the job row, and **the last Channel to finish is what makes the job terminal** (`_finalize_if_complete`) now that no `run_sync_job` sits above them. An enqueue **rings the worker over `NOTIFY`** rather than draining locally, and the 30s sweep is the backstop for a lost ring.
  - **Six lanes, and the drain is weighted rather than ordered.** *Enforced: `tests/services/test_lane_draining.py`, `tests/services/test_sync_lanes.py`.* Ticket 12 completes decision 27's product — `{auto_sync, manual_bulk, manual_single} x {normal, best_effort}` — and `services/sync_lanes.py` holds both the names and the policy, as a **pure transform**, so the fairness property is testable without a queue behind it. `LaneScheduler` is strict between tiers and weighted 3:2:1 within one, favouring single, then bulk, then automatic. **Strict order within a tier is the obvious implementation and is the failure decision 29 names**: a steady trickle of single syncs never empties, so auto-sync never runs and the deployment quietly stops updating while the worker looks busy. The weighting is smooth (nginx's smooth WRR) rather than "three, then two, then one", because the picks are separated by however long a real sync takes and a burst of one Budget is the thing being prevented. Credit only moves for lanes that had work at that moment, so an idle lane cannot bank a burst. The three best-effort lanes exist with **nothing enqueueing onto them** — ticket 23 selects a tier — which is not a mechanism with no caller: the consumer is the caller, and `pgmq.read` raises on a queue that was never created.
    - **A slot is filled the moment it frees, and that resolved both inherited head-of-line notes.** Ticket 10 read a batch per lane and awaited it as a unit, so one Channel in a deep backfill held its slot until its batch finished and no other lane was read until that lane drained; ticket 11 left a coalesced waiter holding a concurrency permit while it waited, so N requests for one busy Channel occupied N scraping slots to scrape nothing. Both are the same object, so one change closes both: `drain_sync_lanes` takes a permit, dispatches **one** message, and comes back, and the permit is a `SyncSlot` the waiter can put down (`slot.released()`) and re-take before its next claim attempt. **It still cannot deadlock** — ticket 11's argument was "the holder takes its permit before it can claim", and that half is untouched: a claim holder never releases while it walks, and only a waiter releases, so no permit is held by something waiting for a claim held by something waiting for a permit. `COALESCE_MAX_WAIT_SECONDS` keeps its value and **loses its original reason**: the gate justification is gone, and what remains is that the waiter's message is claimed under a ~2.4h visibility timeout it must not outlast, and that a person is on the SSE stream.
    - **"Interleaved across Users" is done at the read, not at the enqueue.** *Enforced: `test_lane_draining.py::test_one_accounts_backlog_does_not_block_another_accounts_work`.* A deliberate departure from decision 31's wording that honours its stated reason. Every enqueue call carries exactly one account (`enqueue_sync_job(job, user_id)`), so interleaving there can only reorder within one call — and the failure the decision names is "a user following 500 channels would otherwise block everyone behind them", which is *across* calls. PGMQ is FIFO by `msg_id`, so the two only meet at the read. `_read_interleaved` issues one `pgmq.read` per account with due work, using PGMQ's own `conditional` filter, and merges round-robin; below two accounts it does not engage at all. **Not `pgmq.read_grouped_rr`**, which exists and looks like exactly this: it only reads from groups whose *head* message is visible, so claiming one of an account's messages makes the rest unreadable until it is archived — one sync at a time per account.
    - **An Admin can pause or drain one lane** (`services/sync_lane_control.py`, an orchestrator). **Pause** stops the worker reading that lane and is lossless — the messages stay queued *and visible*, so a resume waits out no timeout. **Drain** means purge: archive everything on the lane, then `cancel_job` every job those messages belonged to. Cancelling is not tidiness — since ticket 10 a job is terminal only when its last Channel finishes, so archiving 40 of a 50-message batch would otherwise leave the job non-terminal for ever and `has_active_sync_job()` answering True, which makes auto-sync skip every tick from then on. Drain deliberately does **not** mean "run this lane now": the enqueue ring plus the 30s sweep already are that. Gated on `Permission.JOBS_MANAGE` rather than a new constant, which already covers enabling a job and already admits to being destructive.
  - **One sync per Channel at a time, and the claim is a row.** *Enforced: `tests/services/test_channel_mutual_exclusion.py`.* Ticket 11. Two backward walks of one Channel interleave their writes to `last_updated`, `anchor_post_id`, `oldest_stored_post_timestamp` and `history_complete_to_cutoff`, and the row that results describes neither walk. Posts are safe without any of this — `bulk_upsert_posts_impl` upserts on the unique constraint — those four are not. The protection was `scraper_jobs._channel_locks`, an `asyncio.Lock` that stopped meaning anything the moment the scheduler left the web process; it is now `Channel.sync_claimed_at` / `sync_claimed_by`, taken by a conditional `UPDATE ... RETURNING` in `services/channels.py`. The lock is **deleted, not kept beside it**: two answers to "is this Channel being synced" diverge the moment the second worker arrives, and which one a call site consulted decides whether the cursors were protected. The claim lives in `sync_single_channel` rather than in its two callers, for ticket 33's reason — that is the function that walks the pages, and guarding a caller leaves the next caller unguarded.
    - **The lease is not the visibility timeout, and the difference is the point.** Five minutes against the VT's ~2.4 hours. The VT decides when a crashed worker's *message* comes back; the lease decides when its *Channel* does. Sizing one off the other leaves a Channel that died at noon refusing every sync until 14:24, including one a person is sitting in front of. A live sync renews on a heartbeat at a third of the lease, so the lease bounds how long a **dead** holder blocks the Channel and never how long a live one may legitimately take — a deep backfill is in no danger of having its claim stolen mid-walk. And because an expired claim is simply taken by the next caller, "a crashed worker's Channel is picked up again" needs no reaper, no sweep and nothing for an operator to run.
    - **Release and renew are conditional on the holder; that is not tidiness.** A runner that overran its lease and had the Channel taken from it still reaches its own `finally`. An unconditional release there clears the **new** holder's claim and hands a third walk into cursors the second is mid-write — the same interleaving, arrived at through the cleanup path instead of the claim path.
    - **The second request rides the first and is not charged.** `_claim_or_coalesce` waits on a `channel_sync_release` notification, with a poll of the claim as the backstop a `NOTIFY` always needs (no replay). It re-attempts the claim every pass, which is both the lost-wakeup fix and the crashed-holder takeover. "Not charged" needed no new code — a coalesced request never enters the walk, so its meter counts nothing and a charge of zero writes no row — but **both halves are asserted**, because either alone passes for a request that scraped and was let off. A lost ring reports the outcome from the row rather than re-claiming: re-claiming there would scrape a Channel that was just scraped, which is the double work coalescing exists to prevent, reached through the one path where nobody is watching.
    - **A waiter holds a concurrency-gate permit while it waits.** Stated rather than glossed: it cannot deadlock, because the holder took its own permit before it could claim, but N requests for one busy Channel occupy N slots. So the wait is capped (`COALESCE_MAX_WAIT_SECONDS`, 15 minutes) and reports a **skip** past it — nothing went wrong, and the Channel is demonstrably being synced. Draining as slots free belongs with ticket 12, which owns the draining strategy; this is the head-of-line shape `sync_queue._batch_size` already documents.
    - **The claim is not the deadline** (decision 33). `next_regular_sync_at` / `next_dynamic_sync_at` are advanced only by `_finalize_channel_success`. Advancing a deadline at claim or enqueue time conflates "enqueued" with "synced", and strands a Channel silently the moment a message exhausts `read_ct` and is archived: the schedule says it ran, and nothing did. Neither claim column is indexed and neither touches `updated_at` or `touch_sync` — every access is by primary key, and bumping the channel-list etag three times per sync for a field the list does not render is churn on the table every page read touches.
  - **One worker per proxy, and the worker count derives from the proxies.** *Enforced: `tests/services/test_proxy_worker_partition.py`.* Ticket 13. The consumer held one `asyncio.Semaphore` sized `min(syncConcurrency, total lane slots)`, which said how many Channels could be walked at once and **nothing about which proxy any of them used** — so a Channel's backward walk hopped proxies page by page and a burst of syncs piled onto whichever lane was least loaded at that instant. It is now a partition (`proxy_pool.build_workers`): one worker per lane slot, pinned to its lane for the whole message, dealt **round-robin across lanes** so that `syncConcurrency` below capacity spreads over distinct proxies instead of stacking three workers on the first one. With no proxies configured the partition is `syncConcurrency` direct workers, which is byte-for-byte the old behaviour.
    - **The worker owns the lane; it does not hold the lane's permit.** What it holds for the whole message is the lane's long-lived `httpx.AsyncClient`. The semaphore permit is still per request, because holding it across a five-minute backfill would park every *other* kind of proxied traffic behind it — thumbnails, bot publishes, probes — and `ProxyPoolManager.acquire` starts raising `ProxyPoolExhausted` at 120s. `hold()` carries that same 120s bound turned around: one bound page fetch queues behind ~20 thumbnails of its own, so unbounded it would stall a message toward its 2.4-hour visibility timeout with nothing in the log. **`sync_queue`'s docstring claims this ticket makes `auto_summary`'s separate semaphore stop mattering, and that is wrong in both halves** — the per-proxy *rate* was never at risk there, since every proxied `fetch_with_retry` has always taken a lane permit; and its *channel* concurrency is still outside the partition, because it calls `run_sync_job` directly and giving it a worker means enqueueing, which inverts its control flow. The forward reference is still open.
    - **A bound worker does not hop, including on retry.** The fallback is the thing a reader will add back, so: hopping moves a dead proxy's load onto the healthy ones at the exact moment Telegram is already pushing back, which is how one rate-limited egress becomes a rate-limited set. "Capacity honestly reflects available proxies" *requires* throughput to drop when a proxy dies. The Channel is not lost — it fails this pass and the next sweep hands it to a worker whose proxy works, because the dead one's worker is parked.
    - **The binding travels by `contextvars`, and it binds the slot rather than the worker.** `core/request_meter.py`'s reason for the first half: the value is set by the consumer and read by the HTTP client, with `sync_orchestrator` and `scraper` in between reading neither. The second half is a bug found while writing it — a coalesced waiter puts its worker down and may take a **different** one back, so a captured worker leaves the walk fetching through a proxy that now belongs to another message, with the partition believing each has its own.
    - **A parked worker must not look like a hung one.** Both transitions log with the capacity that remains, and a drain that finds every worker parked says so and returns rather than blocking — unbounded there means the 30s sweep (APScheduler, `max_instances=1`) skips every tick behind it, so syncing stops with nothing in error. Deliberately **not** a runtime-config field: cooldown lives in `network._bad_proxies`, which is process-local, so a `parked` flag served by the API could never be true. `effectiveProxyCapacity` and `allowedConcurrency` already carry the width.
    - **The pin survives, reshaped.** The partition is built from process-local state, so two replicas would each own every proxy. `test_worker_count.py` asserts the new shape *and* the surviving reason, in both directions — ticket 11's rule that an inverted assertion beats a deleted one.
  - **A status code is Telegram answering; it is not a proxy fault.** *Enforced: `tests/services/test_adaptive_proxy_wait.py`.* Ticket 14. `fetch_with_retry` armed the ten-minute cooldown on any `is_network` exception, and `httpx.HTTPStatusError` subclasses `httpx.HTTPError` — so **a 404 from one deleted channel put its proxy in cooldown**. That predates ticket 13; what ticket 13 changed is the consequence, because a cooldown now parks the bound worker, so on a single-proxy deployment one dead handle stopped dispatch for ten minutes. `services/proxy_pacing.py` (a **pure transform**, for `sync_lanes.py`'s reason) replaces the one predicate with seven named outcomes: only a `TRANSPORT_FAULT` arms cooldown on its own, a `REJECTION` (429/403/5xx) or `SOFT_BLOCK` widens the per-proxy wait, and an `ANSWERED_ERROR` (404, 410, 451) changes nothing at all. The retry predicate is deliberately **unchanged** — it also decides how many attempts a status code gets, which is ticket 08's charging contract, so narrowing it here would quietly re-price the quota ledger while fixing something else.
    - **The wait is the graduated response and cooldown is the top rung of the same ladder.** A 429 means this egress is rate limited; declaring the proxy dead for ten minutes is not proportionate, and on a single-proxy deployment it is indistinguishable from an outage. So a rejection widens (`max(PACE_STEP_MS, wait x 2)`, capped at `PACE_MAX_MS`) and only arms cooldown once the wait is **already** at the ceiling and there is no wider wait left to try. `should_arm_cooldown` reads the pace as it was *before* the failure was folded in, which is why `_record_pace_failure` returns the previous value rather than letting the caller re-read the dict — reading it after arms one rejection early, and the pace never gets a turn at its widest. A **soft block never reaches that rung**: a channel Telegram will not serve on the web view is soft-blocked for every egress equally, so parking a lane over it punishes the proxy for a property of the channel.
      - **Where there is no ladder, a rejection still parks the proxy, and `paced` is a required keyword.** Only web-view requests are paced, so a thumbnail, a publish or a probe never widens the wait and the ceiling is unreachable for it — which would leave a proxy answering 502 on every request in rotation for ever, a regression hiding inside the fix. For that traffic the only two answers are "do nothing" and "cooldown", and an HTTP proxy that has failed upstream commonly announces it with its own 5xx. The rule stays narrower than the one it replaces where that matters: a 404 is an `ANSWERED_ERROR` and parks nothing either way.
    - **Healthy is zero, and narrowing is gradual on purpose.** A deployment nobody is pushing back on sleeps for nothing and stores nothing — the cursor dict stays empty, which is the leak `_prune_expired_cooldowns` already had to fix once. `PACE_NARROW_AFTER_SUCCESSES` consecutive successes narrow the wait one step; snapping back on the first success re-provokes the limit that caused the widening, and the deployment then alternates between hammering Telegram and being refused by it. A floor snap is what makes it actually *reach* zero rather than decaying toward it for ever.
    - **Latency drift is a weak signal, and "weak" is two numbers.** Drift widens by `PACE_DRIFT_FACTOR` (1.25) against a rejection's 2.0, and never past `PACE_DRIFT_MAX_MS` (5s) against the full 30s ceiling — a proxy that is merely slow must not end up throttled as though it were refused. It needs `PACE_DRIFT_MIN_SAMPLES` behind the EMA first, because the first request through a cold lane is always slower than an average that does not exist. Drift is measured **before** the sample joins the EMA: folding first lets a spike drag the mean it is compared against, and does it more the larger the spike is.
    - **`latency` is the request, timed from just before it goes out.** Not from the top of the attempt, which is what it was and is wrong in two ways: it would include the deliberate sleep, and — the one that bites on a healthy deployment — the time spent queued for the lane permit. `hold()`'s docstring notes a page fetch routinely waits behind ~20 thumbnails at the default of one slot, so ordinary contention would read as a latency spike, trip `_is_drifting`, and throttle a proxy Telegram never pushed back on. The pace feeds on this number; it must measure only what Telegram did. The guard for the sleep half has to sleep for real, because with a faked `asyncio.sleep` both versions report roughly zero.
    - **The pace is an interval *between* requests, so the first one never waits.** It reserves the cursor and goes; the second sleeps. `_reserve_pace_turn` is synchronous by construction — no `await` between reading the cursor and writing it back — which is what stops four slots on one lane reading the same "next allowed" moment and leaving together. A cancelled sleep gives its turn back: cancellation reaches a running sync, and the cursor is the one piece of state here with no self-correcting path.
    - **The wait is served *before* the lane permit, not while holding it.** The first cut held it, arguing the permit is what serialises the egress. That was wrong twice over: the cursor already spaces the starts, so the permit added no pacing at all, while holding it parks every *other* kind of traffic on that proxy behind the sleep — at `PACE_MAX_MS` on a one-slot lane, four requests per `ACQUIRE_TIMEOUT_SECONDS`, so thumbnails and publishes start failing with `ProxyLaneUnavailable`. Precisely what ticket 13's `hold()` docstring forbids, reintroduced by the sleep rather than by the walk. **The guard is inverted, not deleted.** Serving it early means the egress must be known before the acquire (`ProxyPoolManager.peek_lane_url` — exact for a bound worker, advisory for free choice), which also fixes the attribution of a failure *during* the acquire: it used to be filed against the `"direct"` egress on a deployment that has none.
    - **A full lane queue is not a proxy fault.** `_proxy_acquire` translated `ProxyPoolExhausted` into a bare `ConnectionError`, which the old rule filed with the dead proxies. That becomes self-reinforcing the moment pacing lands: a paced lane is exactly what makes its own queue deep enough to reach `ACQUIRE_TIMEOUT_SECONDS`, so the mechanism would manufacture the cooldown it exists to replace and blame a healthy proxy on the way. It is now `ProxyLaneUnavailable`, still a `ConnectionError` subclass so nothing above notices.
    - **Two constants, and the one that needed re-deriving was not the obvious one.** `_NO_HEALTHY_WORKER_WAIT_SECONDS` stays 5.0: it bounds the wait for a *free and healthy worker*, and a worker serving a paced fetch is **busy** — not free, not parked — so pacing lengthens the message a worker is already running, which the drain sees through `all_busy()` as ordinary backpressure. Deriving it from `PACE_MAX_MS` would park every empty-queue sweep for thirty seconds for nothing. Its guard needs a worker with a **real lane**: `proxy_url` is None for a laneless one and `is_parked` short-circuits on that, so the first version passed against its own mutation. **`_worst_case_fetch_seconds` did need it** and was missed on the first pass — every attempt may sleep up to `PACE_MAX_MS`, so a call against a proxy at the ceiling is `NETWORK_FETCH_RETRIES x PACE_MAX_MS` longer (240s, ~+28%), and `visibility_timeout_seconds` is built on that number. A VT that under-counts is PGMQ redelivering a message a live worker is still walking, which is decision 32's double-scrape. The 2x factor was absorbing it, so the failure was a silently shrinking margin. Its guard pins the pace term **exactly** rather than as a lower bound, because the backoff term alone (381s) already exceeds it and the loose version survived its own mutation.
    - **Only the web view is paced, and only it supplies signals.** The same `is_telegram_web_url` predicate the quota ledger uses. The Bot API travels the same proxies and answers to different limits, so a publish made to wait thirty seconds because the *web view* is throttled punishes the wrong request — and taking the signal from it would file another service's 429 against this egress's pace.
  - **Ownership is granted by `claim_job`, never by creating a job.** `create_job` runs wherever the request landed, so while it wrote to `_active_jobs` the API believed it owned every sync it created — `get_job` served its own stale object and `apply_progress_event` discarded the worker's deltas as an echo, leaving the stream at `pending` with no `[DONE]` and nothing in error. Only the consumer claims, as it starts a Channel; `_remember` keeps a watcher's reference current without claiming, and caps the mirror at `MAX_MIRRORED_JOBS` because evicting one costs a row read, never correctness.
  - **Scheduler status crosses the split in both directions.** `_job_status` is filled in by whichever process runs the jobs, so `GET /jobs/status` would report `idle`/`null` forever; the worker announces each transition on `scheduler_job_status` and the API folds it in. `POST /jobs/{id}/trigger` **rings** the worker (`request_job_run`) instead of running the runner locally — the alternative is the API tier sweeping retention and fetching `t.me` for a probe sweep on a button press. Announcing is best-effort and swallows its own failures: a job must not be able to fail because its own description could not be sent.
  - **Progress crosses processes over `LISTEN`/`NOTIFY`** (`app/core/pg_notify.py`; *enforced: `tests/services/test_cross_process_progress.py`*). The failure this prevents is silent: `sync_job_events` already falls back to `get_job`'s row read, so a sync in another process would keep streaming and just serve 5-second-old state. The notification therefore carries **the changed Channel**, not only the job id — the row stays on its crash-recovery cadence and the watcher patches a mirror. Cancellation travels the same channel in the other direction, because the `asyncio.Event` the sync polls lives in the worker while `POST /jobs/sync/{id}/cancel` runs in the API.
- **Never hold a session open across `await`ed work.** A transaction left `idle in transaction` pins the xmin horizon, so autovacuum reclaims nothing for as long as it lasts: `run_auto_sync` planned and synced inside one `with Session(engine)` and left `tg_sync_meta` with **10 live rows and 4,743 dead** after 1,062 autovacuums. The symptom is not a slow query — it is single-row updates by primary key that are instant 99% of the time and stall for 21 seconds occasionally, with no I/O and nothing in `pg_blocking_pids`. Read what you need, project it to plain values, close the session, *then* do the slow thing.
- **Sync progress is pushed over SSE**, not polled: `POST /api/v1/jobs/sync` → `GET /api/v1/jobs/sync/{id}/events`. One-shot `GET .../{id}` is the reconnect fallback. The `tg_sync_jobs` row exists only for that fallback — there is **no list endpoint**, so job history is a write-only trail. It is pruned by `SYNC_JOB_RETENTION_DAYS` (a deployment constant, not an operator setting, precisely because nothing browses it) and **only in a terminal state**, so a long sync is never deleted from under a reconnecting client. That makes a job stranded by a restart immortal, which is why `reconcile_interrupted_jobs` runs at startup: in-memory progress does not survive the process, so every non-terminal row is provably dead. It had never been reconciled — 711 rows sat in `running` since June. Since ticket 10 it runs in **the worker's** startup and nowhere else, because the API restarting no longer says anything about whether a sync is alive. **Sound only while the sync tier is single-replica** (see the worker note above).
- **Three layers answer "why is this slow", and they are not interchangeable.** See **Finding slow endpoints** in `deployment.md`. Traefik's JSON access log is the only one that sees *transfer* cost (`backend/scripts/slow_endpoints.py` aggregates it, ranked by total time); `app/middleware/timing.py` gives `Server-Timing` plus a slow-request WARNING keyed by route template; `pg_stat_statements` names the query and its I/O. Reach for the edge log first — this project's bottleneck has moved off the backend before and every server-side number said it was fine. **But the edge log cannot see background work at all**, and after two rounds where it named the culprit it was tempting to stop there: with every endpoint under a second, `pg_stat_statements` still showed the auto-sync tick spending **69 minutes of database time per 10 hours** on stats it discarded (`docs/scheduler-db-cost-plan.md`). Nothing in the top ten by total time was a request. When the layers disagree about whether the system is busy, the one counting requests is the one that is blind.
- **A scheduled job pays its cost every tick, forever.** The endpoint rounds were each triggered by someone waiting; a job nobody is watching has no such signal, so the same "compute it for everything, read one field" defect ran for months. Before batching a computation across every row, check what the caller reads and *when it could possibly matter* — `sync_schedule.needs_dynamic_stats` is that check made explicit, and it took 2,077 channels down to six.
- **Compression is Traefik's job, not the app's.** Deployed responses are gzipped by a `compress` middleware declared on the `backend`/`frontend` service labels in `compose.yml` (see `deployment.md`). Do **not** add Starlette's `GZipMiddleware` to `app/main.py`: it would double-encode behind the proxy, and unlike Traefik it buffers, which *would* stall the SSE routes. `uv run fastapi dev` serves uncompressed — that is expected, and it is why payload sizes look different locally than in the browser against staging.

## Frontend architecture

- **Two API clients, split per *call* by contract (ADR-006).** *Enforced: `src/api/client-split.conform.ts`.* Generated `frontend/src/client/` (committed, do not hand-edit) for the admin/user shell **and** every summarizer call whose response type is at least as useful as a hand-written one. Hand-written `frontend/src/api/` for the rest: SSE streams, blob downloads, and calls whose generated type would be a *downgrade* — either an **open** model (`extra="allow"` → a top-level `[key: string]: unknown`, so `extra` keys arrive as `unknown`) or a **closed but all-optional** one (OpenAPI cannot say "has a default, therefore always present"). The split is per call, not per family: `jobs` has five generated and three hand-written. Measure openness with `string extends keyof T`, never by grepping for `[key: string]` — that also counts *nested* index signatures. After backend API changes, regenerate with `bash scripts/generate-client.sh` (runs with `ENVIRONMENT=production`).
- **Server state = TanStack Query, always.** *Enforced (partially): `src/lib/architecture-invariants.test.ts` pins `DataContext`'s field set, so server state cannot be re-added there.* `DataContext` derives from queries and exposes Dispatch-compatible setters as query-cache write-throughs (`hooks/useChannels.ts`, generic helper `lib/applySetStateAction.ts`); query keys in `hooks/queryKeys.ts`. Add new server state through react-query, not context `useState`.
- **PostgreSQL is the only client-side store.** *Enforced: `src/lib/architecture-invariants.test.ts`.* A4 deleted the IndexedDB mirror (−2,491 lines) — no `idb`/`localforage`/`dexie` dependency, no `indexedDB`, no DB worker. The browser keeps settings and the current selection, nothing else.
- **Nothing says `localStorage` except four modules.** *Enforced: `src/lib/architecture-invariants.test.ts`.* Browser storage is per-account: `lib/storage/scoped.ts` puts every key under `u:<userId>:`, where the id is the JWT `sub` claim **decoded client-side without verifying it** — the namespace is needed synchronously at first render, before `usersReadUserMe()` could resolve, and forging a token buys a prefix, not data. Use `scopedStorage`, never `localStorage`. Roughly thirty keys were bare names before ticket 02, so on a shared machine the second person to sign in inherited the first person's channel selection, filters and settings. The rule is "do not name `localStorage`" rather than "namespace your keys" because only the first can be checked: one forgotten `setItem` in a new hook re-opens the leak for that key silently, and the line looks exactly like the twelve around it.
  - **The four exceptions are device-scoped on purpose, and say why.** `scoped.ts` itself; `theme-provider.tsx` and `api/base.ts`, which read `vite-ui-theme` and `access_token`; and `useAuth.ts`, which owns the token every namespace is derived from. The token cannot be namespaced by the id inside it, and the theme is read by a provider that mounts above the router and renders `/login`, so namespacing it would flash the wrong theme on every sign-in. `DEVICE_SCOPED_KEYS` is asserted as an exact pair — a third entry is a decision, not a drive-by.
  - **Signing out clears the query cache, not just the token.** `logout()` removed the token and nothing else, so every channel, post and summary the previous person had loaded stayed readable until something happened to refetch. `clearStaleSession` had the same hole on the branch where the operator was already on `/login` and the hard `window.location.href` never ran. Stored *preferences* are deliberately **not** cleared: they are already unreachable to another account, and dropping them means signing back in to a reset app.
  - **Values written before the namespace existed are adopted once, by a sweep.** Not key by key at each call site — that only covers the keys somebody remembered. The sweep is marker-guarded per account and **moves** rather than copies, and it refuses to run for a signed-out browser, because filing the existing operator's settings under `u:anon:` would lose them. The first account to sign in after the upgrade therefore claims them; that is deliberate and argued in the module docstring.
  - **The guard matches the identifier, not `localStorage.`** — because `f(localStorage)`, `Object.keys(localStorage)` and the exact line this ticket deleted (`loadAppSettings(typeof window !== "undefined" ? localStorage : …)`) are all member-access-free. Comments and string literals are stripped first, so the prose can keep saying "localStorage" and one export document can keep it as a field name.
  - **A read can never throw.** The sweep writes from inside `getItem`, and `getItem` is called from `useState` initialisers — so a quota error, or Safari with site data blocked (`typeof localStorage` is still `"object"` there and every call throws), would white-screen the app on something that could not fail before. Every accessor swallows storage errors.
  - **Playwright seeds through `tests/utils/scoped-storage.ts`, never bare keys.** The guard only scans `src/`. A spec seeding `hasSeenTour` under its bare name does not merely fail to work — the guided tour opens over the UI and every later assertion in the file misses for reasons unrelated to what it tests. The helper computes the prefix by calling the app's own `scopedKey`, so the namespace format is declared once.
- **Settings = schema-driven.** All browser-persisted settings are declared in `frontend/src/lib/settings/schema.ts` (zod: key, default, legacy keys, backend section); `SettingsContext.tsx` is a thin provider over generated setters, reading and writing through `scopedStorage`. Add settings to the schema, not as new `useState` hooks. Theme is owned by `theme-provider` in `main.tsx` (`localStorage: vite-ui-theme`, declared once in `lib/storage/scoped.ts`) — do not add a second theme toggle (*that one is enforced*; the schema rule is **not** — be careful).
- **Routing/tabs come from the URL.** TanStack Router. The active summarizer tab is the `?tab=` param on `/summarizer` (not localStorage); settings sub-sections use `?section=`.

## Testing & migrations

- **pytest uses a separate database (`app_test`) always** — `tests/conftest.py` overrides `POSTGRES_DB` to it and each test truncates `tg_*` tables afterward. Never point the dev server at `app_test`, and keep `POSTGRES_DB=app` for dev. One-time: `createdb app_test && cd backend && POSTGRES_DB=app_test uv run alembic upgrade head`.
- After changing any model, generate an Alembic revision and commit it; migrations live in `backend/app/alembic/versions/`.
- Maintenance/backfill scripts live in `backend/scripts/` (run with `uv run python backend/scripts/<name>.py`, usually `--dry-run` first) — see `MEMORY.md`.

## Architecture guards — read this before "simplifying" something

The rules above are not all equal. Some are **enforced** by a compile error or a
failing test; the rest are prose and rely on you. Prose decayed here before: this
file said *"never inline `BaseModel` in a route module"* from B1 onward, and three
modules were violating it when the guards below were written. So when a guard
fires, the answer is almost never to delete the guard.

| Guard | Enforces | Kind |
|---|---|---|
| `frontend/src/types.conform.ts` | hand-written domain types match the server | compile error |
| `frontend/src/api/client-split.conform.ts` | the two-client split, **in both directions** | compile error |
| `frontend/src/lib/architecture-invariants.test.ts` | no browser DB; `DataContext` stays small; one theme owner; browser storage has exactly four owners, logout and stale-session both clear the query cache, and the device-scoped key list stays a pair | test |
| `backend/tests/api/test_route_module_hygiene.py` | no models in route modules; handlers annotate returns | test |
| `backend/tests/services/test_service_kinds.py` | every service module declares one of the five kinds | test |
| `backend/tests/api/test_route_inventory.py` | declared routes are actually mounted | test |
| `backend/tests/api/test_*_projection.py` | response key sets — no invented `null`s | test |
| `backend/tests/services/test_photo_cache_lookup_cost.py` | image-cache lookups don't scan the directory, in **both** twin modules | test |
| `backend/tests/services/test_summary_list_payload_cost.py` | listing summaries never opens the corpus table, **and the detail call does** | test |
| `backend/tests/services/test_log_list_payload_cost.py` | log lists drop the bodies; the detail route keeps them; search still reaches them | test |
| `backend/tests/services/test_sync_schedule_stats_narrowing.py` | the scheduler fetches stats only where they can change its answer — and **still fetches them where they can** | test |
| `backend/tests/services/test_sync_job_flush_cost.py` | job progress rides the flush interval; terminal states still write immediately | test |
| `backend/tests/services/test_sync_meta_commit_cost.py` | the etag moves in the same transaction as the change it announces | test |
| `backend/tests/jobs/test_auto_sync_session_scope.py` | the scheduler closes its planning transaction before syncing | test |
| `backend/tests/deployment/test_worker_count.py` | the scheduler has left the API, the API no longer reconciles jobs, enqueueing never drains locally, the sync tier is one replica, the shared concurrency gate became a partition and has not come back beside it | test |
| `backend/tests/services/test_proxy_worker_partition.py` | one worker per proxy slot; a cut partition spreads instead of stacking; a cooling proxy's worker parks and comes back; a bound walk never hops, even when its own proxy fails; the binding is per task and follows a slot that swapped workers; a drain with every proxy parked returns instead of blocking | test |
| `backend/tests/services/test_adaptive_proxy_wait.py` | a 404 neither parks its proxy nor paces it, at the **production** retry count; a transport fault still does; a soft block and a full lane queue are not filed with the dead proxies; the classification is total and its default is inert; the wait widens multiplicatively, stops at a ceiling, and converges back to zero over a *run* of successes; drift is weaker, capped lower, measured before its own sample, and never lowers an existing wait; the wait is served **outside** the lane permit, spaces concurrent requests, gives its turn back when cancelled, and is excluded from the latency it feeds on; cooldown is the ceiling rung, the call site asks in that order, and traffic with no ladder still parks a proxy that rejects it; only the web view is paced; a paced worker is busy rather than parked; the visibility timeout counts the pace exactly | test |
| `backend/tests/services/test_channel_mutual_exclusion.py` | one sync per Channel, enforced outside process memory; a live claim is not stolen and an expired one is; release and renew refuse a claim that is not theirs; the second request rides the first without scraping or being charged; the claim never moves the schedule | test |
| `backend/tests/services/test_cross_process_progress.py` | a watcher sees progress the row does not have yet; creating a job does not claim it; a row re-read cannot walk a Channel backwards; a cancel reaches the running process | test |
| `backend/tests/services/test_sync_lanes.py` | every lane the worker drains was created by a migration; a lane name is its Budget and tier; six lanes are the full product; a trickle never starves auto-sync and an idle lane banks no credit | test |
| `backend/tests/services/test_lane_draining.py` | the weighting and the tier rule hold under **real load**, not as configured constants; new normal work preempts a best-effort backlog mid-drain; one account's backlog does not block another's, and a single account pays nothing for it; a slow message on one lane does not hold up another; a coalesced waiter's permit is genuinely free while it waits; a paused lane keeps its messages visible; a purge cancels the jobs it orphans, including a partly-purged batch | test |
| `backend/tests/core/test_pg_notify.py` | notifications cross a connection boundary and fan out to every subscriber | test |
| `backend/tests/services/test_sync_job_retention.py` | sync jobs are pruned by age but never while unfinished; restarts reconcile | test |
| `backend/tests/api/test_public_route_exemptions.py` | routes with no auth dependency are middleware-public, **and** every exemption still has such a route | test |
| `backend/tests/api/test_permission_checks.py` | authorisation reads roles, never `is_superuser` or a role name; the superuser kept its access | test |
| `backend/tests/services/test_channel_creation_paths.py` | every Channel-creation path writes a Follow; the follow table has one writer; the guard's own function names still exist | test |
| `backend/tests/services/test_settings_table_split.py` | every settings key is classified with a reason; the two tables refuse each other's keys; the sync **and retention** carves are partitions; one writer per table | test |
| `backend/tests/jobs/test_retention_split_four_ways.py` | the corpus sweep ignores who scraped the row; one account's log window never reaches another's; sync and network logs and ownerless rows run on the deployment window and nothing else does; report caps are per account | test |
| `backend/tests/services/test_unfollow.py` | removal drops the follow and nothing else; a foreign channel is 404; a second account's Posts survive | test |
| `backend/tests/jobs/test_retention_collects_unfollowed.py` | retention collects only the Channels nobody follows, with their dependent rows; the test helper still writes a follow | test |
| `backend/tests/services/test_tenancy_seam.py` | every table is classified or excused with a reason; scoping is byte-identical while the flag is off; the flag has exactly one reader | test |
| `backend/tests/services/test_artifact_tenancy_scoping.py` | all four artifact families and the History scope by owner, per leg; a foreign row is 404 with that family's own detail; writes and deletes refuse a foreign row **even while the flag is off**, while lists stay unfiltered; your own row still reaches you | test |
| `backend/tests/services/test_post_tenancy_scoping.py` | the feed, lookup, counts and Discover scope in **both** query shapes; the followed-channel set is scoped too; probes stay corpus and say why; all four reads demand a `user_id` with no default | test |
| `backend/tests/services/test_sync_log_channel_telemetry.py` | a second Follower sees telemetry the first produced; the stamp is not what makes it visible; search and `searchInDetails` stay inside the Follow; the row stores no owner even when handed one; the write checks the Follow, not the owner | test |
| `backend/tests/services/test_import_write_scoping.py` | an import never overwrites another account's row, in **both** flag states; the refusal matches what that family's own endpoint answers for an absent id; ownerless rows stay writable while the flag is off; every table the import writes is checked or excused, derived from the AST; **only reads use the flag-gated ownership guard** | test |
| `backend/tests/services/test_discover_dismissals_are_per_account.py` | the dismissal key carries its owner; a second account can dismiss what the first already dismissed; undoing never reaches another account's row; both `isIgnored` sites answer for the viewer — all of it under **both** flag states | test |
| `backend/tests/services/test_credential_tenancy_scoping.py` | both credential lists read through the seam, not a hand-rolled owner filter; `user_id` is a required keyword; a second account's rows are absent under enforcement and present without it; an ownerless row's fate is pinned in both flag states | test |
| `backend/tests/services/test_setting_group_and_job_scoping.py` | the setting-group list reads through the seam and is **unfiltered** with the flag off; the orphan rescue reaches groups through the follow and not through every Channel; the lookup map stays unscoped and says why, with no owner it would ignore; the running-job read is the caller's in both directions, and the in-memory candidates are filtered by the seam's rule rather than the write path's; all three doors refuse a foreign group in **both** flag states with the family's own 404 string, while an ownerless one stays writable only with the flag off; the old visibility filter is gone and the name-collision one still rejects a duplicate under enforcement; the scope/name unique index is still there | test |
| `backend/tests/services/test_auto_publish_scoping.py` | a foreign credential is refused before it is decrypted, and answers as an absent one does; the destination is checked too; a refusal reaches the publish log instead of returning quietly; the send is attributed to the Summary's owner, pinned on the one row shape that can tell that apart — all of it in **both** flag states | test |
| `backend/tests/services/test_owner_backfill.py` | the migration's frozen table list is the one `SCOPES` derives; no unowned user-owned row survives it; a payload takes its parent's owner rather than the operator's, pinned on the second account that can tell them apart; an orphan owner is adopted like a NULL one and a live owner is left alone; a duplicate setting group is merged with its referrers repointed rather than stamped into a unique-index violation, and one with no counterpart is still adopted; that index is still the only one of its kind; a fresh install completes where a used deployment with no superuser is refused; shared tables keep their NULL stamps — every test passing **on its own**, not only in file order | test |
| `backend/tests/services/test_quota_ledger.py` | one Request per fetch however many attempts; only what reached Telegram; concurrent meters stay apart; the ledger accumulates and nothing prunes it | test |
| `backend/tests/api/test_send_email_call_sites.py` | every `send_email` caller checks `emails_enabled` first — the pair, not the site | test |
| `backend/tests/api/test_approval_gate.py` | every data router requires an approved account; unknown routers fail, exemptions state a reason | test |
| `backend/tests/api/test_registration.py` | signup answers identically for a taken and a free address, and never returns an account | test |
| `backend/tests/core/test_permissions.py` | no stranded permission, no permission on the default role, seeded rows match the constants | test |
| `backend/tests/deployment/test_edge_rate_limit.py` | the auth paths are rate limited at Traefik, and that router keeps its service, priority, and compression | test |
| pre-commit `generate-frontend-sdk` | the committed client matches the backend | hook |

**A fix applied to one of two twin modules is half a fix.** `channel_photos.py` and
`post_thumbnails.py` are the same module twice over (same `_META_SUFFIX`, `_meta_path`,
`_find_image_path`, `has_cached_*`, bounded extension set). The thumb cache was fixed to
probe extensions instead of globbing, with the reasoning in its docstring; the avatar
cache kept the glob for two more months and turned a channel list into 30 seconds. Its
guard is parametrised over *both* modules for that reason — when you fix one of a pair,
guard the pair.

`client-split.conform.ts` is the pattern worth copying. It asserts not only that
the *generated* models stayed closed, but that the *hand-written* ones are still
open — so closing one server-side breaks the build and tells you the call can now
move. **A deliberate exception that nothing checks becomes a leftover nobody dares
touch.** Assert the reason, not just the state.

**Mutation-test every guard before trusting it.** A green suite proves nothing
until you have watched it go red. This caught a false pass in six separate units
of the simplification programme — including one guard that could not fail at all.

## Conventions

- Python: mypy `strict`, `ty check`, ruff (isort, bugbear, no `print` — `T201`). Alembic dir excluded from lint/type-check.
- TS/React: biome, **no semicolons, double quotes**.
- CI test workflows are billing-blocked and never start, so as of 2026-07-30 their `push`/`pull_request` triggers are **commented out** (`grep -rn CI-DISABLED .github/workflows/`; see `.github/workflows/DISABLED.md` for the list and how to re-enable). Expect **no** checks on a PR; run lint/tests locally instead. Only the self-hosted staging deploy runs.
- **Every commit that lands on `main` must be signed** — but that does *not* mean signing every commit. Squash-merging a PR satisfies it automatically: GitHub authors the squash commit and signs it with its own key, so commits on a branch or in a `.claude/worktrees/` worktree need no signature and must never block on one. **Land PRs with squash merge only** — merge-commit mode puts the branch's own commits on `main` as-is, and rebase-merge replays them unsigned. When committing **directly to `main`**, sign locally (1Password); there a signing failure is a blocker to raise, not to bypass with `gpgsign=false`.
- Local `git log %G?` is **not** a valid signature check here — it reports `N` on genuinely SSH-signed commits (`gpg.ssh.allowedSignersFile` is unset) and `E` on GitHub's PGP-signed ones. Audit `main` against GitHub instead: `gh api 'repos/{owner}/{repo}/commits?sha=main&per_page=20' --jq '.[] | select(.commit.verification.verified | not) | .sha'` (empty output = clean).

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).

## Agent skills

### Issue tracker

Local markdown under `.scratch/<feature-slug>/`, **not** GitHub Issues (which is enabled on the remote but unused). See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical labels, unchanged, recorded as a `Status:` line in each issue file. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` at the root, ADRs and `DECISIONS.md` in `docs/migration/` (not `docs/adr/`). See `docs/agents/domain.md`.
